---
phase: 04-game-modes-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/l10n/app_en.arb
  - lib/core/l10n/app_es.arb
  - lib/features/game/game_session_notifier.dart
  - test/features/home/home_screen_test.dart
  - test/features/game/phase4_test.dart
  - test/features/map/grand_master_sequence_test.dart
autonomous: true
requirements:
  - MODE-01
  - MODE-02
  - MODE-03
  - MODE-04
  - MODE-05
  - GAME-07
  - SCOR-05
  - SCOR-06

must_haves:
  truths:
    - "ARB strings for all Phase 4 UI (mode names, HUD, hints, completion, PB banner) are committed and flutter gen-l10n compiles without errors"
    - "GameSessionNotifier.useHint() decrements hintsRemaining and returns false when count is already 0"
    - "RED test stubs exist for HomeScreen, star rating, useHint, and grand master sequence — all fail as expected"
  artifacts:
    - path: "lib/core/l10n/app_en.arb"
      provides: "English ARB with Phase 4 keys"
      contains: "homeTitle"
    - path: "lib/features/game/game_session_notifier.dart"
      provides: "useHint() method"
      contains: "bool useHint"
    - path: "test/features/game/phase4_test.dart"
      provides: "RED stubs for star rating + useHint"
    - path: "test/features/map/grand_master_sequence_test.dart"
      provides: "RED stub for grand master sequence"
    - path: "test/features/home/home_screen_test.dart"
      provides: "RED stubs for HomeScreen rendering"
  key_links:
    - from: "lib/core/l10n/app_en.arb"
      to: "lib/generated/l10n/app_localizations.dart"
      via: "flutter gen-l10n"
      pattern: "homeTitle|hintButton|completionPersonalBest"
---

<objective>
Lay the Wave 1 foundation: add all Phase 4 ARB strings (so every later plan can reference localised text), implement GameSessionNotifier.useHint(), and create RED test stubs that define the acceptance contracts for HomeScreen, star rating, hint logic, and grand master sequencing.

Purpose: Every downstream plan (04-02 through 04-05) depends on the ARB strings being generated and on useHint() existing in the notifier. The RED stubs document expected behaviours before implementation exists, locking the contracts.
Output: Updated ARB files, regenerated l10n classes, useHint() in GameSessionNotifier, and three RED test files.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-game-modes-scoring/04-CONTEXT.md

<interfaces>
<!-- From lib/features/game/game_session_notifier.dart -->
class GameSessionNotifier extends AsyncNotifier<GameSession> {
  // Existing methods:
  void startGame(GameMode mode)
  void recordDrop(String isoCode, {required bool isCorrect})
  Future<void> completeGame()
  void pauseGame()
  void resumeGame()
  // Phase 4 addition — to be created in this plan:
  bool useHint()   // returns true if hint consumed; false if hintsRemaining == 0
}

<!-- From lib/features/game/game_session.dart -->
class GameSession {
  final int hintsRemaining;
  GameSession copyWith({int? hintsRemaining, ...})
}

<!-- From lib/core/data/game_state_repository.dart — pattern for saveSession -->
abstract interface class GameStateRepository {
  Future<void> saveSession(GameSession session);
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 4 ARB strings and regenerate l10n</name>
  <files>lib/core/l10n/app_en.arb, lib/core/l10n/app_es.arb</files>
  <read_first>
    - lib/core/l10n/app_en.arb (read existing keys to know where to append; avoid duplicates)
    - lib/core/l10n/app_es.arb (read existing Spanish keys to match structure)
  </read_first>
  <action>
Append the following keys to lib/core/l10n/app_en.arb (inside the JSON object, before the closing brace). Each key must include a @-annotation object (description only; add "placeholders" where the key has a parameter). The keys to add, verbatim:

  "homeTitle": "Flags Around the World",
  "modeSectionTitle": "Choose your challenge",
  "modeLearnName": "Learn",
  "modeLearnDescription": "Names on map and flag card",
  "modeFlagsMasterName": "Flags Master",
  "modeFlagsMasterDescription": "Name under flag card only",
  "modeGeoMasterName": "Geographical Master",
  "modeGeoMasterDescription": "Names on map only",
  "modeGrandMasterName": "Grand Master",
  "modeGrandMasterDescription": "No names anywhere",
  "homeBestScore": "Best: {score}",
  "@homeBestScore": { "placeholders": { "score": { "type": "int" } } },
  "homeNoBestScore": "Best: —",
  "hintButton": "Hint ×{count}",
  "@hintButton": { "placeholders": { "count": { "type": "int" } } },
  "hintOutTitle": "No hints left",
  "hintOutMessage": "Watch an ad to refill your hints?",
  "hintWatchAd": "Watch Ad",
  "hintCancel": "Cancel",
  "hintAdFailed": "No ad available right now.",
  "completionPersonalBest": "New Personal Best!",
  "completionDone": "Done"

Each of the non-parameterised keys also needs a minimal @-annotation (description string). Use descriptive but short strings, e.g. "@homeTitle": {"description": "App title on home screen"}.

For app_es.arb, add matching Spanish translations. Use these values:
  "homeTitle": "Banderas del Mundo",
  "modeSectionTitle": "Elige tu desafío",
  "modeLearnName": "Aprender",
  "modeLearnDescription": "Nombres en el mapa y en la tarjeta",
  "modeFlagsMasterName": "Maestro de Banderas",
  "modeFlagsMasterDescription": "Nombre solo bajo la tarjeta",
  "modeGeoMasterName": "Maestro Geográfico",
  "modeGeoMasterDescription": "Nombres solo en el mapa",
  "modeGrandMasterName": "Gran Maestro",
  "modeGrandMasterDescription": "Sin nombres en ningún lado",
  "homeBestScore": "Récord: {score}",
  "@homeBestScore": { "placeholders": { "score": { "type": "int" } } },
  "homeNoBestScore": "Récord: —",
  "hintButton": "Pista ×{count}",
  "@hintButton": { "placeholders": { "count": { "type": "int" } } },
  "hintOutTitle": "Sin pistas",
  "hintOutMessage": "¿Ver un anuncio para recargar pistas?",
  "hintWatchAd": "Ver anuncio",
  "hintCancel": "Cancelar",
  "hintAdFailed": "No hay anuncio disponible ahora.",
  "completionPersonalBest": "¡Nuevo récord personal!",
  "completionDone": "Listo"

After editing both ARB files, run: cd C:/code/Claude/FlagsRoundTheWorld && flutter gen-l10n

The command must exit 0. If it fails, inspect the error — most likely a malformed JSON or missing @-annotation — and fix before continuing.
  </action>
  <verify>
    <automated>cd C:/code/Claude/FlagsRoundTheWorld && flutter gen-l10n && flutter analyze lib/generated/l10n/ --no-fatal-infos</automated>
  </verify>
  <done>
    - app_en.arb contains all 22 new Phase 4 keys with @-annotations
    - app_es.arb contains matching Spanish translations
    - flutter gen-l10n exits 0 with no errors
    - lib/generated/l10n/app_localizations.dart compiles without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add useHint() to GameSessionNotifier and create RED test stubs</name>
  <files>lib/features/game/game_session_notifier.dart, test/features/home/home_screen_test.dart, test/features/game/phase4_test.dart, test/features/map/grand_master_sequence_test.dart</files>
  <read_first>
    - lib/features/game/game_session_notifier.dart (full read — already in context above; confirm _gameStateRepository field name before referencing)
    - lib/features/game/game_session.dart (confirm copyWith signature for hintsRemaining)
    - test/features/map/flag_sequence_test.dart (see existing test pattern for grand master stub)
  </read_first>
  <behavior>
    useHint() contract:
    - When hintsRemaining > 0: decrement by 1 via copyWith, persist via _gameStateRepository?.saveSession(), return true
    - When hintsRemaining == 0: return false immediately, do NOT modify state
    - When state.value is null: return false

    RED stub behaviour (all stubs must FAIL with a clear message):
    - home_screen_test: 2 tests that call fail('SC1 not implemented — RED state')
    - phase4_test: 4 star-rating tests + 2 useHint tests that call fail('not implemented — RED state')
    - grand_master_sequence_test: 2 sequence tests that call fail('MODE-05 not implemented — RED state')
  </behavior>
  <action>
In lib/features/game/game_session_notifier.dart, add the useHint() method after resumeGame(). The method signature is:

  bool useHint() {
    final current = state.value;
    if (current == null || current.hintsRemaining <= 0) return false;
    state = AsyncData(current.copyWith(hintsRemaining: current.hintsRemaining - 1));
    _gameStateRepository?.saveSession(state.value!);
    return true;
  }

No new imports are needed — _gameStateRepository is already in scope.

Create test/features/home/home_screen_test.dart (create directory test/features/home/ if it does not exist):

  import 'package:flutter_test/flutter_test.dart';
  void main() {
    group('HomeScreen', () {
      test('SC1: renders 4 mode cards', () => fail('SC1 not implemented — RED state'));
      test('SC1: shows personal best score or dash when no best', () => fail('SC1 not implemented — RED state'));
    });
  }

Create test/features/game/phase4_test.dart (directory test/features/game/ already exists):

  import 'package:flutter_test/flutter_test.dart';
  void main() {
    group('star rating', () {
      test('D-D01: first game (null previousBest) returns 3 stars', () => fail('D-D01 not implemented — RED state'));
      test('D-D02: new PB (score < previousBest) returns 3 stars', () => fail('D-D02 not implemented — RED state'));
      test('D-D02: within 20% of PB returns 2 stars', () => fail('D-D02 not implemented — RED state'));
      test('D-D02: worse than 20% of PB returns 1 star', () => fail('D-D02 not implemented — RED state'));
    });
    group('useHint', () {
      test('GAME-07: useHint decrements hintsRemaining', () => fail('GAME-07 not implemented — RED state'));
      test('GAME-07: useHint returns false when hintsRemaining is 0', () => fail('GAME-07 not implemented — RED state'));
    });
  }

Create test/features/map/grand_master_sequence_test.dart:

  import 'package:flutter_test/flutter_test.dart';
  void main() {
    group('Grand Master sequence', () {
      test('MODE-05: buildGrandMasterSequence returns all 196 ISO codes', () => fail('MODE-05 not implemented — RED state'));
      test('MODE-05: sequence has no duplicates', () => fail('MODE-05 not implemented — RED state'));
    });
  }

IMPORTANT: The ads_isolation_test.dart checks that no file in lib/features/game/ imports from features/ads/. The only file modified here is game_session_notifier.dart — confirm it contains no new "features/ads/" imports before saving.
  </action>
  <verify>
    <automated>cd C:/code/Claude/FlagsRoundTheWorld && flutter test test/features/game/phase4_test.dart test/features/home/home_screen_test.dart test/features/map/grand_master_sequence_test.dart 2>&1 | grep -E "FAILED|Some tests failed|All tests passed"</automated>
  </verify>
  <done>
    - useHint() method exists in GameSessionNotifier with correct decrement-and-persist logic
    - All 3 RED stub test files exist with failing tests (output shows "Some tests failed" or each test listed as FAILED)
    - ads_isolation_test.dart still passes: cd C:/code/Claude/FlagsRoundTheWorld && flutter test test/architecture/ads_isolation_test.dart
    - flutter analyze lib/features/game/game_session_notifier.dart exits with no errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ARB file → gen-l10n | Generated code reflects only what is in committed ARB files; malformed JSON would silently produce wrong output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | app_en.arb JSON structure | mitigate | flutter gen-l10n exits 0 as automated gate; malformed JSON causes gen failure caught in verify step |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; flutter gen-l10n is a build tool already present |
</threat_model>

<verification>
After all tasks complete:
1. cd C:/code/Claude/FlagsRoundTheWorld && flutter gen-l10n
2. cd C:/code/Claude/FlagsRoundTheWorld && flutter analyze lib/ --no-fatal-infos
3. cd C:/code/Claude/FlagsRoundTheWorld && flutter test test/architecture/ads_isolation_test.dart
4. cd C:/code/Claude/FlagsRoundTheWorld && flutter test test/features/game/phase4_test.dart test/features/home/home_screen_test.dart test/features/map/grand_master_sequence_test.dart (expect: all FAIL — RED state is correct)
</verification>

<success_criteria>
- app_en.arb and app_es.arb each contain all 22 new Phase 4 keys
- flutter gen-l10n exits 0; generated AppLocalizations exposes homeTitle, hintButton, completionPersonalBest, and all other new keys
- GameSessionNotifier.useHint() returns true and decrements hintsRemaining when count > 0; returns false when count == 0
- ads_isolation_test.dart remains GREEN
- 10 RED stub tests exist across 3 test files and fail with explicit "RED state" messages
</success_criteria>

<output>
Create `.planning/phases/04-game-modes-scoring/04-01-SUMMARY.md` when done
</output>
