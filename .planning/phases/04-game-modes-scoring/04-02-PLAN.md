---
phase: 04-game-modes-scoring
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/app.dart
  - lib/features/map/map_screen.dart
  - lib/features/home/home_screen.dart
autonomous: true
requirements:
  - MODE-01
  - MODE-02
  - MODE-03
  - MODE-04
  - SCOR-07

must_haves:
  truths:
    - "App launches to HomeScreen (not MapScreen directly)"
    - "HomeScreen shows 4 mode cards — one per GameMode — with mode name, description, and personal best (or '—' when null)"
    - "Tapping a mode card navigates to MapScreen passing the correct GameMode"
    - "MapScreen accepts a required 'mode' constructor parameter and calls startGame(mode) on init"
    - "GoRouter is wired: / → HomeScreen, /play/:mode → MapScreen, /result → CompletionScreen (placeholder route)"
  artifacts:
    - path: "lib/app.dart"
      provides: "MaterialApp.router with GoRouter config"
      contains: "MaterialApp.router"
    - path: "lib/features/home/home_screen.dart"
      provides: "HomeScreen widget with 4 mode cards"
      contains: "HomeScreen"
    - path: "lib/features/map/map_screen.dart"
      provides: "MapScreen with required GameMode mode param"
      contains: "required this.mode"
  key_links:
    - from: "lib/app.dart"
      to: "lib/features/home/home_screen.dart"
      via: "GoRouter '/' route"
      pattern: "HomeScreen"
    - from: "lib/features/home/home_screen.dart"
      to: "lib/features/map/map_screen.dart"
      via: "context.go('/play/${mode.name}')"
      pattern: "go.*play"
    - from: "lib/features/map/map_screen.dart"
      to: "lib/features/game/game_session_notifier.dart"
      via: "ref.read(gameSessionProvider.notifier).startGame(widget.mode)"
      pattern: "startGame.*widget.mode"
---

<objective>
Wire GoRouter navigation into the app, create the HomeScreen mode-selection UI, and add the required GameMode constructor parameter to MapScreen so the correct mode flows from selection through to game start.

Purpose: HomeScreen is the new navigation root (D-A01). Without it, the four game modes cannot be selected. MapScreen must accept the mode param before any mode-specific rendering can be added in Plan 04-04.
Output: A working navigation stack: App launches to HomeScreen → user taps mode card → MapScreen starts that mode. CompletionScreen placeholder route is registered but not yet fully implemented (that is Plan 04-05).
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-game-modes-scoring/04-CONTEXT.md

<interfaces>
<!-- From lib/features/game/game_mode.dart -->
enum GameMode { learn, flagsMaster, geographicalMaster, grandMaster }

<!-- From lib/core/data/high_score_repository.dart -->
abstract interface class HighScoreRepository {
  Future<int?> getBestScore(GameMode mode);
  Future<void> saveBestScore(GameMode mode, int score);
}
// highScoreRepositoryProvider is added by Plan 04-03 (same wave, no conflict).
// HomeScreen must reference it but Plan 04-03 creates it. Since 04-02 and 04-03
// run in parallel and DO NOT share files, the executor must add a forward-reference
// comment in HomeScreen that will compile once Plan 04-03 has run. Alternatively:
// define a temporary inline FutureProvider<HighScoreRepository> in home_screen.dart
// and remove it after 04-03 runs. PREFERRED: define highScoreRepositoryProvider
// in home_screen.dart as a private local provider, then Plan 04-03 promotes it to
// high_score_repository.dart. (See action for chosen approach.)

<!-- From lib/features/map/map_screen.dart (current) -->
class MapScreen extends ConsumerStatefulWidget {
  const MapScreen({super.key});   // ← Phase 4 changes this to require GameMode mode
  // _initSequence(List<CountryData> countries) — currently calls buildFlagSequence();
  // Phase 4 adds a postFrameCallback call to startGame(widget.mode) here.
}

<!-- From lib/features/map/completion_screen.dart (current) -->
class CompletionScreen extends StatelessWidget {
  final GameSession session;
  const CompletionScreen({super.key, required this.session});
  // Phase 4-05 rewrites this; the /result route in GoRouter must accept extra
  // as Map<String, dynamic> with keys 'session' and 'previousBest'.
}

<!-- From lib/generated/l10n/app_localizations.dart (generated, read after 04-01 runs) -->
// Keys used in this plan:
// AppLocalizations.of(context).homeTitle
// AppLocalizations.of(context).modeSectionTitle
// AppLocalizations.of(context).modeLearnName
// AppLocalizations.of(context).modeLearnDescription
// AppLocalizations.of(context).modeFlagsMasterName
// AppLocalizations.of(context).modeFlagsMasterDescription
// AppLocalizations.of(context).modeGeoMasterName
// AppLocalizations.of(context).modeGeoMasterDescription
// AppLocalizations.of(context).modeGrandMasterName
// AppLocalizations.of(context).modeGrandMasterDescription
// AppLocalizations.of(context).homeBestScore(score)
// AppLocalizations.of(context).homeNoBestScore
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire GoRouter in app.dart and create HomeScreen skeleton</name>
  <files>lib/app.dart, lib/features/home/home_screen.dart</files>
  <read_first>
    - lib/app.dart (full read — replace MaterialApp with MaterialApp.router)
    - lib/features/map/completion_screen.dart (read current constructor — /result route must match it)
    - lib/generated/l10n/app_localizations.dart (verify homeTitle and mode name keys exist after 04-01)
  </read_first>
  <action>
Rewrite lib/app.dart. Replace MaterialApp with MaterialApp.router. Import go_router (already in pubspec.yaml as ^17.2.3 — no pub get needed). Remove the SpikeMapScreen FAB.

The router configuration:
  - Route '/': HomeScreen (const constructor)
  - Route '/play/:mode': MapScreen(mode: GameMode.values.byName(state.pathParameters['mode']!))
  - Route '/result': CompletionScreen with extra cast — read extra as Map<String, dynamic>; pass session: extra['session'] as GameSession and previousBest: extra['previousBest'] as int?. Note: CompletionScreen currently has only a 'session' param — add previousBest as an optional int? param in the CompletionScreen constructor NOW (a minimal addition) so the route compiles. Plan 04-05 will rewrite the body; the constructor addition here is non-breaking.

Exact GoRouter instantiation at file scope (top-level final, not inside build):
  final _router = GoRouter(routes: [...]);

MaterialApp.router uses routerConfig: _router and keeps localizationsDelegates and supportedLocales.

Create lib/features/home/home_screen.dart. The file must NOT import from lib/features/ads/ (ads_isolation_test.dart scope does not cover features/home/, but follow the same convention).

HomeScreen is a ConsumerStatefulWidget. In initState, for each of the 4 GameMode values, call getBestScore() on a HighScoreRepository instance. Because highScoreRepositoryProvider is created in Plan 04-03 (parallel plan, different file — high_score_repository.dart), define a private local provider in home_screen.dart for now:

  final _homeHighScoreRepoProvider = FutureProvider<HighScoreRepository>((ref) async {
    final prefs = await SharedPreferences.getInstance();
    return SharedPreferencesHighScoreRepository(prefs);
  });

Plan 04-03 promotes this to a canonical location; after both plans merge, there will be two providers pointing to the same implementation. The executor of Plan 04-04 will remove the private one when wiring MapScreen (which imports from high_score_repository.dart directly). This avoids a compile conflict between parallel plans.

HomeScreen.build returns a Scaffold with:
  - AppBar(title: Text(l10n.homeTitle))
  - Body: ref.watch(_homeHighScoreRepoProvider).when(
      loading: () => const CircularProgressIndicator(),
      error: (e, _) => Text('Error: $e'),
      data: (repo) => _buildModeList(context, repo),
    )

_buildModeList builds a ListView of 4 mode cards. Each card is a Card widget containing a ListTile:
  - title: Text(modeName)
  - subtitle: Text(modeDescription)
  - trailing: FutureBuilder<int?>(
      future: repo.getBestScore(mode),
      builder: (ctx, snap) => Text(snap.hasData && snap.data != null
        ? l10n.homeBestScore(snap.data!)
        : l10n.homeNoBestScore),
    )
  - onTap: () => context.go('/play/${mode.name}')

Mode card data (mode → name → description), per D-A03/D-A04:
  - GameMode.learn → modeLearnName → modeLearnDescription
  - GameMode.flagsMaster → modeFlagsMasterName → modeFlagsMasterDescription
  - GameMode.geographicalMaster → modeGeoMasterName → modeGeoMasterDescription
  - GameMode.grandMaster → modeGrandMasterName → modeGrandMasterDescription

Required imports for home_screen.dart:
  flutter/material.dart, flutter_riverpod, go_router,
  flags_around_the_world/generated/l10n/app_localizations.dart,
  flags_around_the_world/features/game/game_mode.dart,
  flags_around_the_world/core/data/high_score_repository.dart,
  shared_preferences/shared_preferences.dart
  </action>
  <verify>
    <automated>cd C:/code/Claude/FlagsRoundTheWorld && flutter analyze lib/app.dart lib/features/home/home_screen.dart --no-fatal-infos</automated>
  </verify>
  <done>
    - lib/app.dart uses MaterialApp.router with GoRouter; no more MaterialApp(home:...)
    - GoRouter has 3 routes: /, /play/:mode, /result
    - lib/features/home/home_screen.dart compiles; shows 4 mode cards with personal best display
    - flutter analyze reports no errors on both files
  </done>
</task>

<task type="auto">
  <name>Task 2: Add GameMode mode param to MapScreen and call startGame on init</name>
  <files>lib/features/map/map_screen.dart</files>
  <read_first>
    - lib/features/map/map_screen.dart (full read — already in context; key section is _initSequence and the class constructor)
  </read_first>
  <action>
Change the MapScreen class constructor to require a GameMode mode parameter:

  class MapScreen extends ConsumerStatefulWidget {
    final GameMode mode;
    const MapScreen({super.key, required this.mode});
    ...
  }

Add the import for GameMode at the top of the file:
  import 'package:flags_around_the_world/features/game/game_mode.dart';

In _initSequence, add a postFrameCallback that calls startGame AFTER _fitMapToScreen. The current postFrameCallback only calls _fitMapToScreen; change the block at the end of _initSequence so it reads:

  WidgetsBinding.instance.addPostFrameCallback((_) {
    _fitMapToScreen();
    ref.read(gameSessionProvider.notifier).startGame(widget.mode);
  });

The _initSequence body still calls buildFlagSequence for now — Plan 04-03 will add flag_sequence.dart and Plan 04-04 will wire the Grand Master variant. Leave the sequence logic unchanged here.

Also remove the SpikeMapScreen import and FAB references from map_screen.dart if any remain (they were in app.dart; check map_screen.dart for any spike_map_screen.dart imports and remove them).

Do NOT change _advanceToNextFlag, _handleDrop, _buildMap, or any other method. This task is strictly the constructor param + startGame call.
  </action>
  <verify>
    <automated>cd C:/code/Claude/FlagsRoundTheWorld && flutter analyze lib/features/map/map_screen.dart --no-fatal-infos</automated>
  </verify>
  <done>
    - MapScreen constructor has required GameMode mode parameter
    - _initSequence calls startGame(widget.mode) in a postFrameCallback after _fitMapToScreen
    - flutter analyze lib/features/map/map_screen.dart reports no errors
    - ads_isolation_test still passes: cd C:/code/Claude/FlagsRoundTheWorld && flutter test test/architecture/ads_isolation_test.dart
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path parameter → GameMode | /play/:mode parameter is parsed with GameMode.values.byName() — an invalid mode string throws StateError |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | /play/:mode path parameter | accept | App is offline, no external URL routing; mode values are enum-controlled and can only come from HomeScreen taps in practice. StateError on invalid mode string is acceptable for this phase. |
| T-04-02-02 | Denial of Service | HighScoreRepository SharedPreferences read on HomeScreen load | accept | Local read only; failure is caught by FutureProvider error state and displayed as non-crashing error text |
| T-04-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs; go_router already in pubspec.yaml |
</threat_model>

<verification>
After all tasks complete:
1. cd C:/code/Claude/FlagsRoundTheWorld && flutter analyze lib/ --no-fatal-infos
2. cd C:/code/Claude/FlagsRoundTheWorld && flutter test test/architecture/ads_isolation_test.dart
3. cd C:/code/Claude/FlagsRoundTheWorld && flutter build apk --debug (confirms router wiring compiles end-to-end)
</verification>

<success_criteria>
- App entry point is MaterialApp.router; HomeScreen is the '/' route
- HomeScreen renders 4 mode cards (Learn, Flags Master, Geographical Master, Grand Master) with descriptions and personal-best display
- Tapping a mode card navigates to /play/learn (or appropriate mode name)
- MapScreen constructor requires GameMode mode and calls startGame(widget.mode) during initialisation
- flutter analyze reports zero errors across lib/
- ads_isolation_test.dart remains GREEN
</success_criteria>

<output>
Create `.planning/phases/04-game-modes-scoring/04-02-SUMMARY.md` when done
</output>
