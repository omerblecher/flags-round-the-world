---
context: phase
phase: 04-game-modes-scoring
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-05-29T08:45:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Executor main-tree writes | Executor agents wrote files to the main checkout path in addition to the worktree path, causing merge conflicts. | advisory | Before merging worktree: run `git status --short`, reset conflicting files with `git checkout HEAD -- <file>`, remove untracked planning files, THEN merge. |

<current_state>
Phase 4 execution is complete — all 5 plans executed and merged to main. We are at Plan 04-05, Task 3: the blocking human-verify checkpoint.

4 post-execution bug fixes were applied during a video review session but are NOT yet committed:
- lib/features/game/game_hud.dart — HUD height 48→36px, text 12px, LinearProgressIndicator constrained to 6px (fixes yellow line artifact + oversized HUD)
- lib/features/game/flag_tray.dart — Tooltip on hint button explaining what it does (+5 pts penalty)
- lib/features/map/map_screen.dart — Snackbar context bug fixed (capture ScaffoldMessenger before Navigator.pop)
- lib/features/game/game_session_notifier.dart — Hint penalty: _hintPenalty += 5 per hint, included in score formula every tick

The user has NOT yet run the app to verify the 6 checkpoint checks with these fixes applied.
</current_state>

<completed_work>
All Phase 4 plans executed this session:

- Plan 04-01 (Wave 1): ARB strings (22 keys EN+ES), GameSessionNotifier.useHint(), 10 RED test stubs — DONE
- Plan 04-02 (Wave 2, parallel): GoRouter wired, HomeScreen with 4 mode cards, MapScreen required GameMode param — DONE
- Plan 04-03 (Wave 2, parallel): WorldMapPainter showLabels/countryNames, FlagTray hint button, flag_sequence.dart + buildGrandMasterSequence(), grand_master_order.json, highScoreRepositoryProvider, countryNamesProvider — DONE
- Plan 04-04 (Wave 3): lib/core/ads/ migration, GameHud widget, HighlightPainter hintIso, full MapScreen wiring (HUD, per-mode visibility, hint flow, GoRouter completion nav) — DONE
- Plan 04-05 (Wave 4, auto tasks): CompletionScreen rewrite (stars, PB overlay, confetti, computeStarCount()), all RED→GREEN tests — DONE

Post-execution video review fixes (UNCOMMITTED):
- Fix #2: Yellow line below HUD — LinearProgressIndicator constrained
- Fix #3: Hint tooltip added
- Fix #4: Snackbar context bug (ScaffoldMessenger captured before pop)
- Fix #5: Hint penalty +5 pts per hint
- Fix #6: HUD size reduced
</completed_work>

<remaining_work>
1. Commit the 4 bug fixes (see next_action below)
2. Run `flutter run` and verify all 6 checkpoint checks (see checkpoint details below)
3. Type "approved" to complete the Plan 04-05 checkpoint
4. Run /gsd:code-review 4
5. Run /gsd:verify-work 4
</remaining_work>

<decisions_made>
- Hint penalty: +5 pts per hint via `_hintPenalty` accumulator field in GameSessionNotifier. Included in `_onTick` formula: `score = (_elapsedSeconds ~/ 10) + (errorCount * 5) + _hintPenalty`. Reset to 0 on `startGame`.
- HUD: height 48→36px, both text labels 12px (was default bold — too large on device)
- Snackbar fix: capture `final messenger = ScaffoldMessenger.of(context)` and `final failMsg = AppLocalizations.of(context).hintAdFailed` BEFORE `Navigator.pop(context)`. The dialog's BuildContext is detached after pop.
- Main menu polish deferred to Phase 5 (Session Polish & Accessibility) — the plain ListTile layout is functional but not engaging.
</decisions_made>

<blockers>
None technical. Human checkpoint is the only blocker.
</blockers>

## Checkpoint Details (Plan 04-05, Task 3)

Run: `cd C:/code/Claude/FlagsRoundTheWorld && flutter run`

- CHECK 1: App opens to HomeScreen; 4 mode cards; "Best: —" on first launch
- CHECK 2: Mode name visibility — Learn (both), Flags Master (name only), Geo Master (labels only), Grand Master (neither)
- CHECK 3: Grand Master first flag = Japan (JP)
- CHECK 4: HUD shows score | progress bar | timer, all updating
- CHECK 5: Hint ×2 → zooms + highlights 3s → ×1 → ×0 → dialog → "Watch Ad" → snackbar "No ad available right now."
- CHECK 6: Stars + PB overlay on completion; Done → HomeScreen; score updates on mode card

## Required Reading (in order)
1. `.planning/phases/04-game-modes-scoring/04-05-PLAN.md` — checkpoint task details
2. `.planning/phases/04-game-modes-scoring/04-04-SUMMARY.md` — what Wave 3 built
3. `.planning/phases/04-game-modes-scoring/04-05-SUMMARY.md` — what Wave 4 built

## Infrastructure State
- All worktrees cleaned up (git worktree list shows only main)
- All 5 plan SUMMARY.md files committed and on main
- 4 bug fix files modified but uncommitted

<context>
Phase 4 execution was fully completed in one session. The human checkpoint at the end of Plan 04-05 is the final gate before Phase 4 is officially done. During the checkpoint review, the user ran the app and found 6 issues. Issues #2 (yellow line), #3 (hint tooltip), #4 (snackbar not showing), #5 (no hint penalty), and #6 (HUD too big) were all fixed in code. The fixes compile clean (`flutter analyze` passes). The fixes just need to be committed and verified in the running app.

Issue #1 (main menu not engaging) was acknowledged as Phase 5 scope — not a defect.
</context>

<next_action>
Start with:
```
cd C:/code/Claude/FlagsRoundTheWorld
git add lib/features/game/flag_tray.dart lib/features/game/game_hud.dart lib/features/game/game_session_notifier.dart lib/features/map/map_screen.dart
git commit -m "fix(04): HUD size, hint tooltip, snackbar context bug, hint penalty (+5 pts)"
flutter run
```
Then verify all 6 checkpoint checks and type "approved" if they pass.
After approval: /gsd:code-review 4 → /gsd:verify-work 4
</next_action>
