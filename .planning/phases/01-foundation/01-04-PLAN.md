---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - lib/features/ads/ad_load_state.dart
  - lib/features/ads/ad_service.dart
  - test/architecture/ads_isolation_test.dart
  - test/unit/country_data_test.dart
  - test/unit/country_data_service_test.dart
autonomous: true
requirements:
  - COMP-01
  - COMP-03
  - I18N-02
  - I18N-03
must_haves:
  truths:
    - "Ad stub exposes AdLoadState (sealed: AdLoaded, AdFailed) and AdService abstract interface"
    - "StubAdService always returns AdLoadState.failed for all three ad types"
    - "Architecture test confirms zero imports of features/ads/ from game/map/core layers"
    - "Unit test loads world_map_paths.json from assets and asserts 196 entries — zero network calls"
    - "Unit test loads countries_en.json and asserts 196 entries with non-empty names"
    - "Unit test proves locale swap: loading countries_es.json serves Spanish names without Dart changes"
  artifacts:
    - path: "lib/features/ads/ad_load_state.dart"
      provides: "sealed class AdLoadState with AdLoaded and AdFailed variants"
      contains: "sealed class AdLoadState"
    - path: "lib/features/ads/ad_service.dart"
      provides: "abstract AdService interface + StubAdService implementation"
      contains: "abstract interface class AdService"
    - path: "test/architecture/ads_isolation_test.dart"
      provides: "dart:io file walk asserting zero features/ads/ imports in game/map/core"
      contains: "features/ads/"
    - path: "test/unit/country_data_test.dart"
      provides: "196-entry assertion on world_map_paths.json"
      contains: "196"
    - path: "test/unit/country_data_service_test.dart"
      provides: "locale fallback test for I18N-02 and I18N-03"
      contains: "loadCountryNames"
  key_links:
    - from: "test/architecture/ads_isolation_test.dart"
      to: "lib/features/game/, lib/features/map/, lib/core/"
      via: "dart:io Directory.listSync(recursive: true)"
      pattern: "features/ads/"
    - from: "test/unit/country_data_test.dart"
      to: "lib/core/models/country_data.dart"
      via: "CountryData.fromJson()"
      pattern: "CountryData.fromJson"
---

<objective>
Create the ad walled-garden stub and all unit/architecture test files. This plan runs in parallel with Plan 03 (pipeline) and Plan 05 (i18n) — all three depend only on Plan 02's scaffold.

Purpose: The ad stub (D-11) and architecture enforcement test (D-12) are the walled-garden pillars that prevent COPPA violations from accidental ad SDK imports. The unit tests form the automated verification harness required by VALIDATION.md Wave 0. Both sets of files must exist for the full test suite to pass at the end of Phase 1.

Output: Sealed AdLoadState + AdService stub committed; three test files committed and green.
</objective>

<execution_context>
@C:\Users\omerb\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omerb\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<interfaces>
<!-- Extracted from RESEARCH.md §Code Examples (AdLoadState stub) and §Pattern 4 (architecture test) -->

AdLoadState (RESEARCH.md §Dart: AdLoadState Stub):
  sealed class AdLoadState — base class
  class AdLoaded extends AdLoadState — const constructor
  class AdFailed extends AdLoadState — const constructor

AdService (abstract interface):
  abstract interface class AdService
  Future<AdLoadState> loadBannerAd()
  Future<AdLoadState> loadInterstitialAd()
  Future<AdLoadState> loadRewardedAd()

StubAdService implements AdService:
  All three methods return: async => const AdFailed()
  const constructor: const StubAdService()

Architecture test pattern (RESEARCH.md §Pattern 4):
  dart:io Directory.listSync(recursive: true) on:
    - 'lib/features/game'
    - 'lib/features/map'
    - 'lib/core'
  Assert zero .dart files contain "features/ads/"

CountryDataService (lib/core/data/country_data_service.dart — already created in Plan 02):
  Future<List<CountryData>> loadMapData()
  Future<Map<String, String>> loadCountryNames(Locale locale)

CountryData (lib/core/models/country_data.dart — already created in Plan 02):
  factory CountryData.fromJson(Map<String, dynamic> json)
  String isoCode, List<String> pathStrings, List<Path> paths, BoundingBox boundingBox, Offset centroid
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ad walled-garden stub (D-11) and architecture enforcement test (D-12)</name>
  <files>
    lib/features/ads/ad_load_state.dart,
    lib/features/ads/ad_service.dart,
    test/architecture/ads_isolation_test.dart
  </files>
  <read_first>
    lib/features/ads/ad_load_state.dart (may be empty or .gitkeep — read current state)
    lib/features/ads/ad_service.dart (may be empty or .gitkeep — read current state)
    .planning/phases/01-foundation/01-RESEARCH.md §Dart: AdLoadState Stub
    .planning/phases/01-foundation/01-RESEARCH.md §Pattern 4: Architecture Enforcement Test
    .planning/phases/01-foundation/01-CONTEXT.md §D-11, D-12
  </read_first>
  <behavior>
    - `AdLoadState` is a sealed class (not an enum) so Phase 6 can add data fields to AdLoaded
    - `AdFailed` is the only state StubAdService ever returns
    - Architecture test passes green immediately (no game/map/core files exist yet that could import ads)
    - Architecture test fails if any file in lib/features/game/, lib/features/map/, or lib/core/ contains the string "features/ads/"
    - Architecture test runs with `flutter test test/architecture/ads_isolation_test.dart` (not a standalone dart test)
  </behavior>
  <action>
    Step 1 — Create `lib/features/ads/ad_load_state.dart`:
    Define a sealed class `AdLoadState` with const constructor. Define `class AdLoaded extends AdLoadState` with const constructor. Define `class AdFailed extends AdLoadState` with const constructor. No imports needed (pure Dart). The sealed modifier ensures exhaustive switch matching in Phase 6.

    Step 2 — Create `lib/features/ads/ad_service.dart`:
    Import `ad_load_state.dart` (relative import). Define `abstract interface class AdService` with three abstract methods: `Future<AdLoadState> loadBannerAd()`, `Future<AdLoadState> loadInterstitialAd()`, `Future<AdLoadState> loadRewardedAd()`.
    Define `class StubAdService implements AdService` with a `const StubAdService()` constructor. All three methods return `async => const AdFailed()`.

    Step 3 — Create `test/architecture/ads_isolation_test.dart`:
    Use the pattern from RESEARCH.md §Pattern 4 exactly. Import `dart:io` and `package:flutter_test/flutter_test.dart`. The test description must be: `'game, map, and core layers have no imports from features/ads/'`. The directories to check are: `'lib/features/game'`, `'lib/features/map'`, `'lib/core'`. The pattern to search for in file contents is the string `"features/ads/"`. Use `directory.existsSync()` guard before listing (directories may be empty .gitkeep-only). Collect violations list. Assert `violations.isEmpty` with reason message.

    Run the test immediately:
    `flutter test test/architecture/ads_isolation_test.dart`
    It must exit 0 (green). If it fails, diagnose why before proceeding.
  </action>
  <verify>
    <automated>flutter test test/architecture/ads_isolation_test.dart --reporter=compact</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/architecture/ads_isolation_test.dart` exits 0 (test passes)
    - `grep "sealed class AdLoadState" lib/features/ads/ad_load_state.dart` outputs a match
    - `grep "class AdLoaded extends AdLoadState" lib/features/ads/ad_load_state.dart` outputs a match
    - `grep "class AdFailed extends AdLoadState" lib/features/ads/ad_load_state.dart` outputs a match
    - `grep "abstract interface class AdService" lib/features/ads/ad_service.dart` outputs a match
    - `grep "StubAdService implements AdService" lib/features/ads/ad_service.dart` outputs a match
    - `grep "AdFailed" lib/features/ads/ad_service.dart` outputs a match (stub always returns AdFailed)
    - `grep "features/ads" lib/core/data/country_data_service.dart` outputs NO match
    - `grep "features/ads" lib/main.dart` outputs NO match
  </acceptance_criteria>
  <done>
    Ad walled-garden stub committed. Architecture enforcement test green. COPPA isolation enforced from first commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit tests for CountryData model and CountryDataService locale behavior</name>
  <files>
    test/unit/country_data_test.dart,
    test/unit/country_data_service_test.dart
  </files>
  <read_first>
    lib/core/models/country_data.dart (read the actual fromJson signature — created in Plan 02)
    lib/core/data/country_data_service.dart (read loadMapData and loadCountryNames signatures — created in Plan 02)
    .planning/phases/01-foundation/01-VALIDATION.md §Wave 0 Requirements
    .planning/phases/01-foundation/01-VALIDATION.md §Per-Task Verification Map
    .planning/phases/01-foundation/01-RESEARCH.md §Pattern 5 (CountryDataService locale fallback)
    assets/map/world_map_paths.json (read first 3 entries to confirm JSON structure — created in Plan 03)
    assets/data/countries_en.json (read first 3 entries to confirm structure — created in Plan 03)
  </read_first>
  <behavior>
    country_data_test.dart tests:
    - CountryData.fromJson() parses a single well-formed country entry → isoCode, pathStrings, paths, boundingBox, centroid all non-null
    - parseSvgPathData("M0,0 L10,0 L10,10 Z") returns a non-empty dart:ui Path
    - CountryDataService.loadMapData() (via rootBundle in test binding) returns a List with exactly 196 entries
    - Every entry has non-empty isoCode (2 lowercase chars), at least 1 pathString, valid boundingBox (w>0, h>0), valid centroid

    country_data_service_test.dart tests:
    - loadCountryNames(Locale('en')) returns a map with 196 entries, all values non-empty (I18N-02)
    - loadCountryNames(Locale('es')) returns a map with 196 entries and Spanish names (I18N-02)
    - loadCountryNames(Locale('de')) falls back to English names when countries_de.json is absent (I18N-03: no Dart code change needed — graceful fallback)
    - Both en and es maps have the same set of keys
  </behavior>
  <action>
    Step 1 — Read `lib/core/models/country_data.dart` and `lib/core/data/country_data_service.dart` to understand exact signatures before writing tests.

    Step 2 — Create `test/unit/country_data_test.dart`:
    - Import `package:flutter_test/flutter_test.dart`, `package:path_drawing/path_drawing.dart`, and the app's `country_data.dart`.
    - Use `TestWidgetsFlutterBinding.ensureInitialized()` in `setUpAll` to enable rootBundle in tests.
    - Group 1: "CountryData.fromJson" — create a minimal inline JSON map with iso='de', paths=['M0,0 L10,0 Z'], boundingBox={x:0,y:0,w:10,h:10}, centroid={x:5,y:5}. Call `CountryData.fromJson(map)`. Assert: isoCode=='de', pathStrings.length==1, paths.length==1, boundingBox.w==10, centroid.dx==5.
    - Group 2: "path_drawing parseSvgPathData" — call `parseSvgPathData('M0,0 L10,0 L10,10 Z')`. Assert result is a `Path` (using `isA<Path>()`). Assert result is not empty (convert to metrics and check length > 0).
    - Group 3: "CountryDataService.loadMapData" (requires flutter test binding + rootBundle):
      Instantiate `CountryDataService()`. Call `loadMapData()` (await). Assert list length == 196. Assert all entries have isoCode.length == 2. Assert all entries have pathStrings.isNotEmpty. Assert all entries have boundingBox.w > 0 and boundingBox.h > 0. Assert 'xk' is in the returned ISO codes. Assert 'tw' is in the returned ISO codes.
      NOTE: This test requires assets to be registered. Run with `flutter test` (not `dart test`) so rootBundle is available.

    Step 3 — Create `test/unit/country_data_service_test.dart`:
    - Import `package:flutter_test/flutter_test.dart`, `dart:ui` (for Locale), and the app's `country_data_service.dart`.
    - Use `TestWidgetsFlutterBinding.ensureInitialized()` in `setUpAll`.
    - Test 1 "loadCountryNames English": instantiate `CountryDataService()`, call `loadCountryNames(const Locale('en'))`, assert result.length == 196, assert all values are non-empty strings, assert result['de'] == 'Germany', assert result['xk'] == 'Kosovo', assert result['tw'] == 'Taiwan'.
    - Test 2 "loadCountryNames Spanish": call `loadCountryNames(const Locale('es'))`, assert result.length == 196, assert result.containsKey('de'), assert result['de'] != 'Germany' (Spanish name is different), assert result.containsKey('xk').
    - Test 3 "loadCountryNames unknown locale falls back to English" (I18N-03): call `loadCountryNames(const Locale('de'))` (German locale, no countries_de.json exists). Assert result.length == 196 (English base returned). Assert result['de'] == 'Germany' (English fallback). This test MUST NOT throw — the service must catch the missing file silently.
    - Test 4 "English and Spanish key sets are identical": assert that keys from en load == keys from es load (same set of 196 ISO codes).

    Step 4 — Run the tests:
    `flutter test test/unit/country_data_test.dart test/unit/country_data_service_test.dart`
    All tests must be green. Fix any failures before proceeding.

    NOTE on rootBundle in tests: Flutter test runner loads pubspec.yaml assets automatically when run with `flutter test`. If tests fail with "Unable to load asset", verify that the assets are registered in pubspec.yaml (done in Plan 02) and that world_map_paths.json and countries_en.json exist (done in Plan 03). The tests in this plan depend on Plan 03's outputs — run after Plan 03 is complete.
  </action>
  <verify>
    <automated>flutter test test/unit/country_data_test.dart test/unit/country_data_service_test.dart --reporter=compact</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/unit/country_data_test.dart test/unit/country_data_service_test.dart` exits 0
    - All individual test groups pass (CountryData.fromJson, parseSvgPathData, loadMapData, loadCountryNames)
    - The 196-entry assertion passes (no off-by-one from 195 → 196 update per D-09)
    - The unknown-locale fallback test passes WITHOUT throwing (I18N-03 verified)
    - Kosovo (xk) and Taiwan (tw) are confirmed present in both English and Spanish loads
    - `test/unit/country_data_test.dart` exists and has at least 3 test groups
    - `test/unit/country_data_service_test.dart` exists and has at least 4 test groups
  </acceptance_criteria>
  <done>
    All unit tests green. COMP-03 (offline asset load), I18N-02 (196 country names), and I18N-03 (locale swap without Dart changes) are verified by automated tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dart:io file walk | Architecture test reads source files on the local filesystem |
| rootBundle in tests | Flutter test runner provides asset access from pubspec.yaml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-ADS-01 | Information Disclosure | Accidental features/ads/ import in game/map/core | mitigate | ads_isolation_test.dart enforces zero imports; runs on every `flutter test` invocation |
| T-1-ADS-02 | Tampering | StubAdService returning non-failed state | accept | Stub is const and returns only AdFailed; no configuration path to change this without code changes |
| T-1-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs in this plan; all packages already in pubspec from Plan 02 |
</threat_model>

<verification>
1. `flutter test test/architecture/ads_isolation_test.dart` exits 0
2. `flutter test test/unit/country_data_test.dart test/unit/country_data_service_test.dart` exits 0
3. 196-entry assertion passes in country_data_test.dart
4. Locale fallback (I18N-03) passes without throwing
5. `grep "sealed class AdLoadState" lib/features/ads/ad_load_state.dart` returns a match
6. `grep "abstract interface class AdService" lib/features/ads/ad_service.dart` returns a match
</verification>

<success_criteria>
- Ad stub sealed class + interface committed (D-11)
- Architecture enforcement test green (D-12 / COMP-01)
- CountryData model unit test: 196 entries, Kosovo + Taiwan present (COMP-03)
- CountryDataService locale test: English 196 entries, Spanish 196 entries, unknown locale fallback (I18N-02, I18N-03)
- All four tests run without network calls (COMP-03)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
