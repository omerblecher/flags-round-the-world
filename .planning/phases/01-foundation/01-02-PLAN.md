---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - pubspec.yaml
  - pubspec.lock
  - l10n.yaml
  - analysis_options.yaml
  - lib/main.dart
  - lib/app.dart
  - lib/core/models/country_data.dart
  - lib/core/data/country_data_service.dart
  - lib/features/game/.gitkeep
  - lib/features/map/.gitkeep
  - lib/features/home/.gitkeep
  - assets/flags/.gitkeep
  - assets/audio/.gitkeep
  - android/app/src/main/AndroidManifest.xml
  - android/app/build.gradle
autonomous: true
requirements:
  - COMP-01
  - COMP-03
must_haves:
  truths:
    - "Flutter project exists with package ID com.otis.brooke.flags.around.the.world"
    - "Feature-folder skeleton is in place (game, map, ads, home, core)"
    - "All asset directories are registered in pubspec.yaml"
    - "Release AndroidManifest.xml has no uses-permission elements"
    - "All Phase 1 Dart and Flutter dependencies are locked in pubspec.lock"
    - "App launches with `flutter run` (debug mode, no crash on start)"
  artifacts:
    - path: "pubspec.yaml"
      provides: "All Phase 1 dependencies + flutter: generate: true + asset registrations"
      contains: "flutter_riverpod: ^3.3.1"
    - path: "l10n.yaml"
      provides: "gen-l10n configuration with synthetic-package: false"
      contains: "synthetic-package: false"
    - path: "lib/main.dart"
      provides: "ProviderScope root + MaterialApp entry point"
      contains: "ProviderScope"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Release manifest with no permissions"
    - path: "android/app/build.gradle"
      provides: "Correct applicationId com.otis.brooke.flags.around.the.world"
      contains: "com.otis.brooke.flags.around.the.world"
  key_links:
    - from: "lib/main.dart"
      to: "lib/app.dart"
      via: "runApp(ProviderScope(child: App()))"
      pattern: "ProviderScope"
    - from: "pubspec.yaml"
      to: "assets/flags/, assets/map/, assets/data/, assets/audio/"
      via: "flutter: assets:"
      pattern: "assets/flags/"
---

<objective>
Create the Flutter project scaffold — the Walking Skeleton foundation. This is the first vertical slice: a real app that builds, launches, and has the complete directory structure all subsequent phases build on.

Purpose: Establishes the package ID, feature-folder architecture (D-01, D-02, D-03), all dependency versions, Android manifest compliance baseline (COMP-01), and asset registrations (COMP-03). Every other plan in Phase 1 operates inside this scaffold.

Output: A Flutter project that `flutter run` launches offline with no crash. SKELETON.md architectural decisions are implemented.
</objective>

<execution_context>
@C:\Users\omerb\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omerb\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/SKELETON.md
</context>

<interfaces>
<!-- Key decisions the executor must implement exactly. Extracted from CONTEXT.md and RESEARCH.md. -->

Package ID: com.otis.brooke.flags.around.the.world
Min SDK: sdk: '>=3.7.0 <4.0.0', flutter: '>=3.32.0'

pubspec.yaml dependencies (exact versions per RESEARCH.md):
  flutter_riverpod: ^3.3.1
  riverpod_annotation: ^4.0.2
  flutter_svg: ^2.3.0
  go_router: ^17.2.3
  path_drawing: ^1.0.1
  shared_preferences: ^2.5.5
  intl: ^0.20.2
  flutter_localizations: (sdk: flutter)

pubspec.yaml dev_dependencies:
  riverpod_generator: ^4.0.3
  build_runner: ^2.15.0
  mocktail: ^1.0.5
  flutter_test: (sdk: flutter)
  integration_test: (sdk: flutter)

pubspec.yaml flutter section:
  generate: true          # REQUIRED for flutter gen-l10n
  uses-material-design: true
  assets:
    - assets/flags/
    - assets/map/
    - assets/data/
    - assets/audio/

l10n.yaml:
  arb-dir: lib/core/l10n
  template-arb-file: app_en.arb
  output-localization-file: app_localizations.dart
  output-class: AppLocalizations
  output-dir: lib/generated/l10n
  synthetic-package: false
  required-resource-attributes: true
  nullable-getter: false

CountryData fields (D-04, D-07):
  isoCode: String (lowercase, 2-char)
  pathStrings: List<String>
  paths: List<Path>    (parsed from pathStrings via parseSvgPathData)
  boundingBox: BoundingBox (x, y, w, h doubles)
  centroid: Offset (dart:ui)

BoundingBox:
  x, y, w, h: double
  get rect => Rect.fromLTWH(x, y, w, h)
  factory BoundingBox.fromJson(Map<String, dynamic> json)
  factory CountryData.fromJson(Map<String, dynamic> json)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Flutter project scaffold with feature-folder structure and compliant manifests</name>
  <files>
    pubspec.yaml, l10n.yaml, analysis_options.yaml, lib/main.dart, lib/app.dart,
    lib/core/models/country_data.dart, lib/core/data/country_data_service.dart,
    lib/features/game/.gitkeep, lib/features/map/.gitkeep,
    lib/features/home/.gitkeep, lib/features/ads/.gitkeep,
    assets/flags/.gitkeep, assets/map/.gitkeep, assets/data/.gitkeep, assets/audio/.gitkeep,
    android/app/src/main/AndroidManifest.xml, android/app/build.gradle
  </files>
  <read_first>
    .planning/phases/01-foundation/SKELETON.md
    .planning/phases/01-foundation/01-CONTEXT.md §Implementation Decisions D-01 through D-07
    .planning/phases/01-foundation/01-RESEARCH.md §Standard Stack (pubspec starting point)
    .planning/phases/01-foundation/01-RESEARCH.md §Anti-Patterns to Avoid
    .planning/phases/01-foundation/01-RESEARCH.md §Pattern 3 (l10n.yaml)
  </read_first>
  <behavior>
    - flutter create exits 0
    - android/app/build.gradle has applicationId "com.otis.brooke.flags.around.the.world" (set explicitly in Step 1b — flutter create alone produces the wrong ID)
    - android/app/src/main/AndroidManifest.xml has package="com.otis.brooke.flags.around.the.world" (set explicitly in Step 1b)
    - pubspec.yaml contains flutter_riverpod: ^3.3.1 (NOT 2.x)
    - pubspec.yaml flutter section has generate: true
    - pubspec.yaml assets section registers assets/flags/, assets/map/, assets/data/, assets/audio/
    - l10n.yaml has synthetic-package: false
    - android/app/src/main/AndroidManifest.xml has zero uses-permission elements
    - lib/main.dart contains ProviderScope wrapping the app
    - lib/core/models/country_data.dart exports CountryData and BoundingBox with fromJson factories
  </behavior>
  <action>
    Step 1 — Create Flutter project:
    Run `flutter create --org com.otis.brooke.flags --project-name flags_around_the_world .` from the repo root (C:\code\Claude\FlagsRoundTheWorld).

    IMPORTANT: `flutter create --org com.otis.brooke.flags --project-name flags_around_the_world` produces applicationId `com.otis.brooke.flags.flags_around_the_world`, NOT the required `com.otis.brooke.flags.around.the.world`. Step 1b corrects this.

    Step 1b — Fix applicationId and package attribute:
    After flutter create exits 0, explicitly correct the package ID in two files:
    a) Open android/app/build.gradle (or android/app/build.gradle.kts if the project uses Kotlin DSL). Find the defaultConfig block and set applicationId to "com.otis.brooke.flags.around.the.world". Replace any generated value such as "com.otis.brooke.flags.flags_around_the_world".
    b) Open android/app/src/main/AndroidManifest.xml. On the <manifest> element, set the package attribute to package="com.otis.brooke.flags.around.the.world". Replace any generated value.
    These two files are the canonical source of truth for the Android package ID.

    Step 2 — Create directory skeleton (D-01, D-02, D-03):
    Create these directories and .gitkeep files:
    - `lib/features/game/.gitkeep`
    - `lib/features/map/.gitkeep`
    - `lib/features/home/.gitkeep`
    - `lib/features/ads/.gitkeep` (placeholder; ads implementation is next plan)
    - `assets/flags/.gitkeep`
    - `assets/map/.gitkeep`
    - `assets/data/.gitkeep`
    - `assets/audio/.gitkeep`
    - `scripts/` directory (for generate_map.py, next plan)
    - `lib/core/models/`
    - `lib/core/data/`
    - `lib/core/l10n/`
    - `lib/generated/l10n/` (gen-l10n writes here; add to .gitignore)
    - `test/unit/`
    - `test/architecture/`

    Step 3 — Replace pubspec.yaml:
    Replace the generated pubspec.yaml with one containing:
    - name: flags_around_the_world
    - description: Educational flag-placement game
    - publish_to: 'none'
    - version: 1.0.0+1
    - environment: sdk '>=3.7.0 <4.0.0', flutter '>=3.32.0'
    - All dependencies per RESEARCH.md Standard Stack (exact versions in interfaces block above)
    - dev_dependencies per RESEARCH.md
    - flutter: generate: true, uses-material-design: true
    - assets: [assets/flags/, assets/map/, assets/data/, assets/audio/]
    Do NOT include firebase_core, firebase_analytics, or any firebase_* package.

    Step 4 — Create l10n.yaml:
    Create `l10n.yaml` at project root with the exact config from the interfaces block above (synthetic-package: false is critical; arb-dir: lib/core/l10n).

    Step 5 — Create lib/core/models/country_data.dart:
    Implement `BoundingBox` (x, y, w, h doubles; `get rect => Rect.fromLTWH(x, y, w, h)`; `fromJson` factory) and `CountryData` (isoCode String, pathStrings List<String>, paths List<Path>, boundingBox BoundingBox, centroid Offset; `fromJson` factory that calls `parseSvgPathData` from `package:path_drawing/path_drawing.dart` for each path string). Import `dart:ui` for Path and Offset. Import `package:path_drawing/path_drawing.dart`.

    Step 6 — Create lib/core/data/country_data_service.dart:
    Implement `CountryDataService` class with:
    - `Future<List<CountryData>> loadMapData()`: calls `rootBundle.loadString('assets/map/world_map_paths.json')`, JSON-decodes, returns list of CountryData.fromJson entries.
    - `Future<Map<String, String>> loadCountryNames(Locale locale)`: loads `assets/data/countries_en.json` as base, then attempts `assets/data/countries_${locale.languageCode}.json` as overlay (silent catch on missing). Returns merged Map<String, String> keyed by lowercase ISO code, value is country name string. CRITICAL — JSON format is FLAT strings, NOT nested maps. The countries JSON files use the schema {"de": "Germany", "fr": "France"} where each value is a plain string, not a nested object. Parse the base file with `(jsonDecode(enJson) as Map<String, dynamic>).cast<String, String>()`. Do NOT use the `(v as Map)["name"]` nested-map pattern shown in RESEARCH.md Pattern 5 — that pattern expects nested objects and does not match the flat-string format generated by Plan 01-03's pipeline script. The overlay file uses the same flat schema and must be merged with `base[k] = v` where v is already a String.
    Import `dart:convert`, `package:flutter/services.dart`, `dart:ui` (Locale). No network calls. No http import.

    Step 7 — Replace lib/main.dart:
    Replace generated main.dart with: `WidgetsFlutterBinding.ensureInitialized()` then `runApp(const ProviderScope(child: App()))`. Import `package:flutter_riverpod/flutter_riverpod.dart` and `app.dart`.

    Step 8 — Create lib/app.dart:
    Implement `App` StatelessWidget returning `MaterialApp` with title 'Flags Around the World' and `localizationsDelegates` set to `AppLocalizations.localizationsDelegates` and `supportedLocales` to `AppLocalizations.supportedLocales`. Include a placeholder home screen (Scaffold with Text('Phase 1 Scaffold')). The AppLocalizations import path must be `generated/l10n/app_localizations.dart` (relative import from lib/). Add `// TODO: wire GoRouter in Phase 3` comment. Do NOT use `MaterialApp.router` yet — GoRouter wiring is Phase 3. Use plain `MaterialApp` with a placeholder home for now.

    Step 9 — Verify release AndroidManifest.xml:
    Open `android/app/src/main/AndroidManifest.xml`. Remove any `<uses-permission>` elements found. The manifest should only contain `<manifest>`, `<application>`, and `<activity>` tags with the Flutter defaults. Do NOT remove `android:name`, `android:label`, or any `<activity>` configuration — only remove `<uses-permission>` elements if present.

    Step 10 — Add lib/generated/l10n/ to .gitignore:
    Add the line `lib/generated/` to .gitignore so the gen-l10n output is not committed (it is regenerated from ARB files on each build).

    Step 11 — Run flutter pub get:
    Run `flutter pub get` and confirm it exits 0 and generates pubspec.lock.
  </action>
  <verify>
    <automated>flutter pub get && flutter analyze lib/ --no-fatal-infos</automated>
  </verify>
  <acceptance_criteria>
    - `flutter pub get` exits 0; pubspec.lock exists and is non-empty
    - `flutter analyze lib/ --no-fatal-infos` exits 0 (no errors; infos allowed)
    - `grep "com.otis.brooke.flags.around.the.world" android/app/build.gradle` returns a match (correct applicationId, not the flutter create default com.otis.brooke.flags.flags_around_the_world)
    - `grep "flutter_riverpod" pubspec.yaml` outputs a line containing `^3.3.1`
    - `grep "generate: true" pubspec.yaml` outputs a match
    - `grep "firebase" pubspec.yaml pubspec.lock` outputs NO matches
    - `grep "synthetic-package: false" l10n.yaml` outputs a match
    - `grep "uses-permission" android/app/src/main/AndroidManifest.xml` outputs NO matches
    - `grep "ProviderScope" lib/main.dart` outputs a match
    - `grep "parseSvgPathData" lib/core/models/country_data.dart` outputs a match
    - `grep "loadString" lib/core/data/country_data_service.dart` outputs a match
    - Directories exist: lib/features/game/, lib/features/map/, lib/features/ads/, lib/features/home/, lib/core/models/, lib/core/data/, lib/core/l10n/, assets/flags/, assets/map/, assets/data/, assets/audio/, test/unit/, test/architecture/, scripts/
  </acceptance_criteria>
  <done>
    Flutter project builds (flutter pub get exits 0), analyzer is clean, release manifest has no permissions, firebase is absent, applicationId is com.otis.brooke.flags.around.the.world in build.gradle, all directories are in place per D-01/D-02/D-03.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify app launches and passes compliance baseline checks</name>
  <files>
    android/app/src/main/AndroidManifest.xml
  </files>
  <read_first>
    android/app/src/main/AndroidManifest.xml (read current state after flutter create)
    android/app/src/debug/AndroidManifest.xml (verify INTERNET is debug-only)
    pubspec.yaml (verify no firebase)
    .planning/phases/01-foundation/01-RESEARCH.md §Security Domain
  </read_first>
  <action>
    Run the following compliance checks:

    1. `grep -r "firebase" pubspec.yaml pubspec.lock` — must return no matches (COPPA: D-10/ADS-10 prohibition).

    2. `grep "INTERNET" android/app/src/main/AndroidManifest.xml` — must return no matches (release manifest must be clean).

    3. `grep "INTERNET" android/app/src/debug/AndroidManifest.xml` — must return a match (debug manifest should have INTERNET for hot reload — this is expected and correct Flutter default behavior).

    4. `grep "AD_ID" android/app/src/main/AndroidManifest.xml` — must return no matches (AD_ID baseline must be clean; Phase 6 adds the `tools:remove` block when google_mobile_ads is added).

    5. Build the app in debug mode: `flutter build apk --debug`
       - This confirms all Dart code compiles, all pubspec dependencies resolve, and assets are bundled.
       - Build must exit 0.

    6. Verify the output APK exists: `ls build/app/outputs/flutter-apk/app-debug.apk`

    If any compliance check fails, fix the AndroidManifest.xml before proceeding:
    - Remove any stray `<uses-permission>` elements from `android/app/src/main/AndroidManifest.xml`
    - Do NOT remove anything from the debug manifest
  </action>
  <verify>
    <automated>flutter analyze lib/ --no-fatal-infos</automated>
  </verify>
  <acceptance_criteria>
    - `flutter build apk --debug` exits 0
    - `build/app/outputs/flutter-apk/app-debug.apk` exists
    - `grep -r "firebase" pubspec.yaml pubspec.lock` exits with no matches
    - `grep "INTERNET" android/app/src/main/AndroidManifest.xml` exits with no matches
    - `grep "INTERNET" android/app/src/debug/AndroidManifest.xml` exits with a match (INTERNET present in debug manifest)
    - `grep "AD_ID" android/app/src/main/AndroidManifest.xml` exits with no matches
  </acceptance_criteria>
  <done>
    App builds to APK. Android manifest compliance baseline established: no INTERNET or AD_ID in release manifest, no firebase anywhere. Walking Skeleton is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev package resolution | `flutter pub get` fetches packages from pub.dev |
| Android manifest merger | Gradle merges debug+main manifests at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Information Disclosure | Release AndroidManifest.xml INTERNET permission | mitigate | Task 2 acceptance criterion: grep confirms no INTERNET in main manifest |
| T-1-03 | Information Disclosure | Firebase transitive dependency | mitigate | Task 2 acceptance criterion: grep pubspec.yaml pubspec.lock for "firebase" returns no matches |
| T-1-04 | Information Disclosure | AD_ID permission baseline | mitigate | Task 2 acceptance criterion: grep confirms no AD_ID in main manifest |
| T-1-SC | Tampering | flutter pub get package resolution | mitigate | All packages verified Approved in RESEARCH.md Package Legitimacy Audit; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
1. `flutter pub get` exits 0
2. `flutter analyze lib/ --no-fatal-infos` exits 0
3. `flutter build apk --debug` exits 0
4. `grep -r "firebase" pubspec.yaml pubspec.lock` -> no matches
5. `grep "INTERNET" android/app/src/main/AndroidManifest.xml` -> no matches
6. `grep "uses-permission" android/app/src/main/AndroidManifest.xml` -> no matches
7. `grep "com.otis.brooke.flags.around.the.world" android/app/build.gradle` -> match
8. All required directories exist (verified by listing)
</verification>

<success_criteria>
- Flutter project with package ID com.otis.brooke.flags.around.the.world created and builds
- applicationId explicitly set to com.otis.brooke.flags.around.the.world in android/app/build.gradle (per CLAUDE.md requirement)
- Feature-folder skeleton in place (D-01, D-02, D-03)
- All asset directories registered in pubspec.yaml (D-03)
- Release AndroidManifest.xml has zero uses-permission elements (COMP-01)
- Firebase absent from all dependency files (COMP-01)
- CountryData and BoundingBox models exist with fromJson factories (D-04)
- CountryDataService exists with loadMapData() and loadCountryNames(Locale) (I18N-02/I18N-03 architecture)
- Walking Skeleton: app builds to APK
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
