---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - COMP-03
must_haves:
  truths:
    - "Flutter SDK 3.32+ is installed and `flutter` is on PATH"
    - "Python geopandas, shapely, pyproj are installed and importable"
    - "Executor can run `flutter create`, `flutter test`, and `flutter build` without errors"
  artifacts: []
  key_links: []

user_setup:
  - service: flutter-sdk
    why: "Flutter SDK is not in PATH per environment audit. Must be installed before any Flutter tasks can execute."
    env_vars: []
    dashboard_config:
      - task: "Install Flutter SDK 3.32+ and add to PATH"
        location: "https://docs.flutter.dev/get-started/install — download stable channel, add flutter/bin to PATH"
      - task: "Install Android SDK / Android Studio (required for `flutter build apk`)"
        location: "https://developer.android.com/studio — install, run `flutter doctor` to verify"
---

<objective>
Verify all build-time dependencies are installed and available before any code is written.

Purpose: Flutter SDK is confirmed absent from PATH (environment audit). geopandas is not installed. Creating Flutter files or running the Python pipeline without these will cause every subsequent plan to fail immediately. This checkpoint gates Wave 2+ plans on confirmed tool availability.

Output: Human confirms Flutter SDK + Android toolchain installed; Python pipeline deps verified installable.
</objective>

<execution_context>
@C:\Users\omerb\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omerb\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Python pipeline dependencies</name>
  <files>scripts/requirements.txt</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md §Python Pipeline Dependencies
  </read_first>
  <action>
    Run the following to verify or install geopandas and its dependencies:

    1. Check if geopandas is importable: `python -c "import geopandas; print(geopandas.__version__)"`
    2. If not installed, run: `pip install geopandas pyproj shapely`
    3. Re-verify: `python -c "import geopandas, shapely, pyproj; print('OK')"`
    4. Create `scripts/requirements.txt` with these three lines:
       - `geopandas>=0.14.0`
       - `shapely>=2.0.0`
       - `pyproj>=3.6.0`

    This file documents the Python pipeline's dependencies. It is NOT a Flutter/Dart file.
  </action>
  <verify>
    <automated>python -c "import geopandas, shapely, pyproj; print('Pipeline deps OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import geopandas, shapely, pyproj; print('Pipeline deps OK')"` exits 0 and prints "Pipeline deps OK"
    - `scripts/requirements.txt` exists and contains lines for geopandas, shapely, pyproj
  </acceptance_criteria>
  <done>Python pipeline dependencies installable and importable on this machine.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Install Flutter SDK and Android toolchain</name>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md §Environment Availability
  </read_first>
  <what-built>
    Task 1 verified the Python pipeline environment. Flutter SDK is not yet installed (confirmed absent from PATH). Android SDK is required for `flutter build apk`.
  </what-built>
  <how-to-verify>
    1. Go to https://docs.flutter.dev/get-started/install/windows/mobile
    2. Download Flutter SDK (stable channel, 3.32.0 or later)
    3. Extract to a location without spaces (e.g., C:\flutter or C:\Users\omerb\flutter)
    4. Add `C:\flutter\bin` (or equivalent) to your system PATH
    5. Install Android Studio from https://developer.android.com/studio
    6. In Android Studio: SDK Manager → install Android SDK + SDK Platform Tools + Build Tools
    7. Run `flutter doctor` — resolve all issues (accept licenses, etc.)
    8. Confirm these all work:
       - `flutter --version` → shows 3.32.x or later
       - `flutter doctor` → all green or only web/iOS warnings (Android must be green)
       - `dart --version` → shows Dart 3.7.x or later
  </how-to-verify>
  <resume-signal>
    Type "flutter ready" after `flutter --version` shows 3.32+ and `flutter doctor` shows Android toolchain as green.
    If you encounter issues, paste the `flutter doctor` output.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install | Third-party Python packages installed from PyPI |
| Flutter SDK download | Binary download from flutter.dev CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-ENV-01 | Tampering | pip install geopandas/shapely/pyproj | mitigate | All three packages are long-standing, high-download PyPI packages (geopandas 10+ yrs); verify after install with `import` check |
| T-1-ENV-02 | Tampering | Flutter SDK binary download | accept | Downloaded directly from official flutter.dev; SHA256 checksum available on download page; developer installs manually |
| T-1-ENV-SC | Tampering | npm/pip/cargo installs | mitigate | Package legitimacy audit in RESEARCH.md confirms all packages Approved; no [ASSUMED]/[SUS] flags |
</threat_model>

<verification>
- `python -c "import geopandas, shapely, pyproj; print('OK')"` exits 0
- `flutter --version` shows 3.32.x or later
- `flutter doctor` shows Android toolchain green
- `dart --version` shows 3.7.x or later
</verification>

<success_criteria>
- Python pipeline dependencies are installed and importable
- Flutter SDK 3.32+ is on PATH
- Android SDK is configured (`flutter doctor` Android is green)
- All subsequent plans can execute without environment setup
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
