---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - lib/core/l10n/app_en.arb
  - lib/core/l10n/app_es.arb
  - lib/app.dart
autonomous: true
requirements:
  - I18N-01
must_haves:
  truths:
    - "`flutter gen-l10n` runs and exits 0"
    - "Generated AppLocalizations class appears in lib/generated/l10n/app_localizations.dart"
    - "app_en.arb and app_es.arb contain the Phase 1 UI chrome strings"
    - "lib/app.dart references AppLocalizations.localizationsDelegates (no hardcoded English strings)"
    - "All Dart files in lib/ have zero hardcoded English UI strings (only ARB-backed lookups)"
  artifacts:
    - path: "lib/core/l10n/app_en.arb"
      provides: "English ARB template with all Phase 1 UI chrome strings"
      contains: "appTitle"
    - path: "lib/core/l10n/app_es.arb"
      provides: "Spanish ARB translations for all Phase 1 chrome strings"
      contains: "appTitle"
  key_links:
    - from: "lib/app.dart"
      to: "lib/generated/l10n/app_localizations.dart"
      via: "AppLocalizations.localizationsDelegates import"
      pattern: "AppLocalizations"
    - from: "l10n.yaml"
      to: "lib/core/l10n/app_en.arb"
      via: "arb-dir + template-arb-file"
      pattern: "arb-dir: lib/core/l10n"
---

<objective>
Set up the ARB-based i18n infrastructure for UI chrome strings and run `flutter gen-l10n` to generate the type-safe AppLocalizations class.

Purpose: I18N-01 requires all UI chrome strings externalized to ARB files. Phase 1 has minimal UI (just the scaffold placeholder), so this plan establishes the ARB structure and generation pipeline that Phases 2â€“5 will extend. The generated class must be importable from `lib/generated/l10n/app_localizations.dart` (NOT from `package:flutter_gen`).

Output: `app_en.arb` and `app_es.arb` committed with Phase 1 chrome strings; `flutter gen-l10n` exits 0; lib/app.dart wires the delegates.
</objective>

<execution_context>
@C:\Users\omerb\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omerb\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<interfaces>
<!-- l10n.yaml config â€” already created in Plan 02 (read it to confirm) -->
l10n.yaml settings:
  arb-dir: lib/core/l10n
  template-arb-file: app_en.arb
  output-localization-file: app_localizations.dart
  output-class: AppLocalizations
  output-dir: lib/generated/l10n
  synthetic-package: false
  required-resource-attributes: true
  nullable-getter: false

ARB format (required-resource-attributes: true means every entry needs @key metadata):
{
  "@@locale": "en",
  "appTitle": "Flags Around the World",
  "@appTitle": {
    "description": "The application title shown in the app bar"
  },
  "startGame": "Start Game",
  "@startGame": {
    "description": "Button label to start a new game session"
  }
}

Phase 1 UI chrome strings needed (minimal â€” only what lib/app.dart and placeholder home use):
  - appTitle: "Flags Around the World" / "Banderas Alrededor del Mundo"
  - (Additional strings added by Phases 2â€“5 as UI is built)

Import path in Dart (RESEARCH.md Â§Pattern 3 â€” CRITICAL):
  import '../generated/l10n/app_localizations.dart';    // from lib/app.dart
  import '../../generated/l10n/app_localizations.dart'; // from lib/features/*/
  NOT: import 'package:flutter_gen/gen_l10n/app_localizations.dart';

MaterialApp localization wiring:
  localizationsDelegates: AppLocalizations.localizationsDelegates,
  supportedLocales: AppLocalizations.supportedLocales,
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ARB files and run flutter gen-l10n</name>
  <files>
    lib/core/l10n/app_en.arb,
    lib/core/l10n/app_es.arb
  </files>
  <read_first>
    l10n.yaml (read to confirm arb-dir, output-dir, synthetic-package settings from Plan 02)
    pubspec.yaml (confirm flutter: generate: true is present from Plan 02)
    .planning/phases/01-foundation/01-RESEARCH.md Â§Pattern 3 (gen-l10n with synthetic-package: false)
    .planning/phases/01-foundation/01-RESEARCH.md Â§Pitfall 3 (synthetic-package breaking change)
    .planning/phases/01-foundation/01-RESEARCH.md Â§Anti-Patterns (do NOT put country names in ARB)
  </read_first>
  <behavior>
    - app_en.arb has "@@locale": "en" and at minimum the appTitle key with @appTitle metadata
    - app_es.arb has "@@locale": "es" and the same keys as app_en.arb with Spanish translations
    - Both ARB files pass `flutter gen-l10n` with zero errors or warnings
    - Generated file appears at lib/generated/l10n/app_localizations.dart
    - No country names (Germany, France, etc.) appear in any ARB file â€” country names are in JSON assets
  </behavior>
  <action>
    Step 1 â€” Read `l10n.yaml` to confirm configuration from Plan 02. Verify: arb-dir is `lib/core/l10n`, template-arb-file is `app_en.arb`, output-dir is `lib/generated/l10n`, synthetic-package is `false`, required-resource-attributes is `true`.

    Step 2 â€” Create `lib/core/l10n/app_en.arb`:
    Create the file with valid ARB JSON. Include `"@@locale": "en"`. Include the following string keys with their @metadata entries (required-resource-attributes: true demands @key for every key):

    Keys to include in Phase 1:
    - `appTitle` / `@appTitle`: "Flags Around the World" â€” app bar title
    - `loadingMessage` / `@loadingMessage`: "Loading..." â€” shown while assets load
    - `errorLoadingData` / `@errorLoadingData`: "Failed to load map data." â€” error state placeholder
    - `scaffoldHomeLabel` / `@scaffoldHomeLabel`: "Phase 1 Scaffold" â€” placeholder home screen label (removed in Phase 3)

    Do NOT add country names to this file. Do NOT add game-mode strings (Phase 4), score strings (Phase 4), or pause strings (Phase 5) â€” those phases add their own keys.

    Step 3 â€” Create `lib/core/l10n/app_es.arb`:
    Same structure as app_en.arb. `"@@locale": "es"`. Translate:
    - appTitle: "Banderas Alrededor del Mundo"
    - loadingMessage: "Cargando..."
    - errorLoadingData: "Error al cargar los datos del mapa."
    - scaffoldHomeLabel: "Fase 1 Andamiaje"

    Step 4 â€” Run `flutter gen-l10n`:
    Execute `flutter gen-l10n` from the project root. It must exit 0 and produce `lib/generated/l10n/app_localizations.dart` and `lib/generated/l10n/app_localizations_en.dart` and `lib/generated/l10n/app_localizations_es.dart`.

    Step 5 â€” Verify generated output:
    `grep "appTitle" lib/generated/l10n/app_localizations.dart` must return a match.
    `grep "loadingMessage" lib/generated/l10n/app_localizations.dart` must return a match.
    `grep "class AppLocalizations" lib/generated/l10n/app_localizations.dart` must return a match.
  </action>
  <verify>
    <automated>flutter gen-l10n && grep "class AppLocalizations" lib/generated/l10n/app_localizations.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter gen-l10n` exits 0 with no errors
    - `lib/generated/l10n/app_localizations.dart` exists
    - `lib/generated/l10n/app_localizations_en.dart` exists
    - `lib/generated/l10n/app_localizations_es.dart` exists
    - `grep "class AppLocalizations" lib/generated/l10n/app_localizations.dart` returns a match
    - `grep "appTitle" lib/generated/l10n/app_localizations.dart` returns a match
    - `grep "Germany\|France\|United States" lib/core/l10n/app_en.arb` returns NO match (country names must not be in ARB)
    - `lib/core/l10n/app_en.arb` contains "@@locale": "en"
    - `lib/core/l10n/app_es.arb` contains "@@locale": "es"
    - Both ARB files have @metadata entries for every key (required-resource-attributes: true compliance)
  </acceptance_criteria>
  <done>
    ARB files committed. flutter gen-l10n runs successfully. AppLocalizations class generated in lib/generated/l10n/.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire AppLocalizations into lib/app.dart and verify no hardcoded strings</name>
  <files>lib/app.dart</files>
  <read_first>
    lib/app.dart (read current state from Plan 02 â€” may have placeholder localizationsDelegates)
    lib/generated/l10n/app_localizations.dart (read the class signature to get correct import path)
    .planning/phases/01-foundation/01-RESEARCH.md Â§Pattern 3 (import path: generated/l10n/app_localizations.dart)
  </read_first>
  <behavior>
    - lib/app.dart imports AppLocalizations from 'generated/l10n/app_localizations.dart' (relative import, NOT package:flutter_gen)
    - MaterialApp has localizationsDelegates: AppLocalizations.localizationsDelegates
    - MaterialApp has supportedLocales: AppLocalizations.supportedLocales
    - The placeholder home screen text uses AppLocalizations.of(context)!.scaffoldHomeLabel (not a hardcoded English string)
    - flutter analyze lib/ exits 0 after this change
  </behavior>
  <action>
    Step 1 â€” Read `lib/app.dart` to see the current state (created in Plan 02 with a placeholder).

    Step 2 â€” Read `lib/generated/l10n/app_localizations.dart` to confirm the exact import path and class name.

    Step 3 â€” Update `lib/app.dart`:
    - Add import: `import 'generated/l10n/app_localizations.dart';` (relative from lib/)
    - In the MaterialApp widget:
      - Set `title: 'Flags Around the World'` (title is not shown to users; this is the OS task switcher label â€” it can be a string literal here as it is not UI chrome)
      - Add `localizationsDelegates: AppLocalizations.localizationsDelegates,`
      - Add `supportedLocales: AppLocalizations.supportedLocales,`
    - For the placeholder home Scaffold:
      - Use `Builder(builder: (context) => Scaffold(body: Center(child: Text(AppLocalizations.of(context)!.scaffoldHomeLabel))))` so that AppLocalizations.of(context) has a valid context.
      - The Scaffold must NOT have `Text('Phase 1 Scaffold')` as a hardcoded string literal â€” use `AppLocalizations.of(context)!.scaffoldHomeLabel` instead.
    - Ensure the import is NOT `package:flutter_gen/gen_l10n/app_localizations.dart` â€” that path is deprecated.

    Step 4 â€” Run analysis:
    `flutter analyze lib/ --no-fatal-infos`
    Fix any errors. Common issues:
    - "The method 'of' isn't defined for 'AppLocalizations'" â†’ ensure import is correct
    - "Undefined name 'AppLocalizations'" â†’ ensure l10n.yaml and pubspec.yaml have generate: true and the gen-l10n ran (Task 1)

    Step 5 â€” Run a build verify:
    `flutter build apk --debug`
    This confirms the full compilation including generated localization code.
  </action>
  <verify>
    <automated>flutter analyze lib/ --no-fatal-infos</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/ --no-fatal-infos` exits 0 (no errors)
    - `flutter build apk --debug` exits 0
    - `grep "AppLocalizations.localizationsDelegates" lib/app.dart` returns a match
    - `grep "AppLocalizations.supportedLocales" lib/app.dart` returns a match
    - `grep "generated/l10n/app_localizations.dart" lib/app.dart` returns a match
    - `grep "flutter_gen" lib/app.dart` returns NO match (deprecated import path absent)
    - `grep "'Phase 1 Scaffold'" lib/app.dart` returns NO match (no hardcoded UI string)
    - `grep "scaffoldHomeLabel" lib/app.dart` returns a match (ARB-backed string used)
  </acceptance_criteria>
  <done>
    AppLocalizations wired into MaterialApp. No hardcoded English strings in Dart UI code. I18N-01 fully satisfied: all UI chrome externalized to ARB files with type-safe gen-l10n output.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flutter gen-l10n | Flutter SDK tooling reads ARB files and generates Dart code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-I18N-01 | Tampering | ARB files contain hardcoded sensitive content | accept | ARB files contain only UI chrome strings; no PII, no credentials, no country political content |
| T-1-I18N-02 | Information Disclosure | Hardcoded English strings in Dart UI bypass i18n | mitigate | Acceptance criteria: grep confirms no hardcoded string literals in UI positions; flutter analyze catches type errors |
| T-1-SC | Tampering | flutter gen-l10n code generation | accept | Flutter SDK tooling from official source; output is generated Dart code inspectable before use |
</threat_model>

<verification>
1. `flutter gen-l10n` exits 0
2. `lib/generated/l10n/app_localizations.dart` exists with `class AppLocalizations`
3. `flutter analyze lib/ --no-fatal-infos` exits 0
4. `flutter build apk --debug` exits 0
5. No hardcoded English strings in lib/app.dart UI positions
6. No country names in ARB files
</verification>

<success_criteria>
- `flutter gen-l10n` succeeds; AppLocalizations class generated (I18N-01)
- app_en.arb and app_es.arb committed with all Phase 1 chrome strings and @metadata
- lib/app.dart wires localizationsDelegates and supportedLocales (I18N-01)
- No hardcoded English strings in Dart code â€” all UI text via ARB (I18N-01)
- Import path is generated/l10n/app_localizations.dart (not deprecated flutter_gen path)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done
</output>
