---
phase: 01-foundation
plan: 06
type: execute
wave: 5
depends_on:
  - "01-03"
  - "01-04"
  - "01-05"
files_modified: []
autonomous: true
requirements:
  - COMP-01
  - COMP-03
  - I18N-01
  - I18N-02
  - I18N-03
must_haves:
  truths:
    - "Full flutter test suite passes with zero failures"
    - "flutter build apk --debug exits 0"
    - "All five phase success criteria are verified"
    - "No firebase references in pubspec.yaml or pubspec.lock"
    - "Release AndroidManifest.xml has no uses-permission elements"
    - "world_map_paths.json has exactly 196 entries"
    - "countries_en.json and countries_es.json each have exactly 196 entries"
    - "flutter gen-l10n exits 0"
    - "All three test files in test/ are green"
  artifacts:
    - path: "assets/map/world_map_paths.json"
      provides: "196 country geometry entries"
      contains: "\"version\":1"
    - path: "assets/data/countries_en.json"
      provides: "196 English country names"
    - path: "assets/data/countries_es.json"
      provides: "196 Spanish country names"
    - path: "lib/features/ads/ad_load_state.dart"
      provides: "sealed AdLoadState with AdLoaded and AdFailed"
      contains: "sealed class AdLoadState"
    - path: "lib/generated/l10n/app_localizations.dart"
      provides: "Generated type-safe localization class"
      contains: "class AppLocalizations"
  key_links:
    - from: "flutter test"
      to: "test/unit/country_data_test.dart"
      via: "flutter test runner"
      pattern: "196"
    - from: "flutter test"
      to: "test/architecture/ads_isolation_test.dart"
      via: "flutter test runner"
      pattern: "features/ads/"
---

<objective>
Run the full integration verification — all tests, all compliance checks, all five phase success criteria. This plan has no files to create; it only executes the verification harness assembled by Plans 02–05.

Purpose: Phase 1 cannot be marked complete until all five success criteria from ROADMAP.md are satisfied. This plan is the final gate: run every check, confirm the phase goal is met, and record the result.

Output: Verification results documented. Phase 1 marked complete.
</objective>

<execution_context>
@C:\Users\omerb\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omerb\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and compliance checks</name>
  <files></files>
  <read_first>
    .planning/phases/01-foundation/01-VALIDATION.md §Per-Task Verification Map
    .planning/phases/01-foundation/01-VALIDATION.md §Manual-Only Verifications
    .planning/ROADMAP.md §Phase 1 Success Criteria
  </read_first>
  <action>
    Run all automated checks in sequence. Record results. Fix any failures by returning to the relevant plan.

    CHECK 1 — Architecture isolation (D-12, COMP-01):
    `flutter test test/architecture/ads_isolation_test.dart --reporter=compact`
    Expected: 1 test passed.

    CHECK 2 — Unit tests (COMP-03, I18N-02, I18N-03):
    `flutter test test/unit/ --reporter=compact`
    Expected: All tests passed.

    CHECK 3 — Full test suite:
    `flutter test --reporter=compact`
    Expected: All tests passed, 0 failures.

    CHECK 4 — flutter gen-l10n (I18N-01):
    `flutter gen-l10n`
    Expected: exits 0, no errors printed.

    CHECK 5 — Flutter analyzer (code quality):
    `flutter analyze lib/ --no-fatal-infos`
    Expected: exits 0, no errors.

    CHECK 6 — Firebase absent (COMP-01):
    `grep -r "firebase" pubspec.yaml pubspec.lock`
    Expected: no output (no matches).

    CHECK 7 — Release manifest clean (COMP-01):
    `grep "INTERNET\|uses-permission\|AD_ID" android/app/src/main/AndroidManifest.xml`
    Expected: no output (no matches).

    CHECK 8 — Debug manifest has INTERNET (correct Flutter default):
    `grep "INTERNET" android/app/src/debug/AndroidManifest.xml`
    Expected: one match line.

    CHECK 9 — world_map_paths.json count (COMP-03, pipeline success criterion 1):
    `python -c "import json; d=json.load(open('assets/map/world_map_paths.json')); print(len(d['countries']))"` 
    Expected: prints 196.

    CHECK 10 — countries_en.json count (I18N-02, success criterion 4):
    `python -c "import json; d=json.load(open('assets/data/countries_en.json')); print(len(d))"`
    Expected: prints 196.

    CHECK 11 — countries_es.json count (I18N-02):
    `python -c "import json; d=json.load(open('assets/data/countries_es.json')); print(len(d))"`
    Expected: prints 196.

    CHECK 12 — Debug build (COMP-03, success criterion 5):
    `flutter build apk --debug`
    Expected: exits 0.

    If any check fails:
    - Determine which plan the fix belongs to
    - Fix the issue in that plan's files
    - Re-run all checks from CHECK 1
    - Do not proceed to SUMMARY until all 12 checks pass
  </action>
  <verify>
    <automated>flutter test --reporter=compact && flutter analyze lib/ --no-fatal-infos</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test` exits 0 (zero test failures)
    - `flutter analyze lib/ --no-fatal-infos` exits 0 (zero errors)
    - `flutter gen-l10n` exits 0
    - `flutter build apk --debug` exits 0
    - `grep -r "firebase" pubspec.yaml pubspec.lock` returns no output
    - `grep "uses-permission" android/app/src/main/AndroidManifest.xml` returns no output
    - World map JSON has exactly 196 entries (python check)
    - countries_en.json has exactly 196 entries (python check)
    - countries_es.json has exactly 196 entries (python check)
  </acceptance_criteria>
  <done>
    All 12 checks pass. Phase 1 success criteria satisfied.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — app launches offline on device</name>
  <read_first>
    .planning/ROADMAP.md §Phase 1 Success Criteria #5
    .planning/phases/01-foundation/01-VALIDATION.md §Manual-Only Verifications
  </read_first>
  <what-built>
    Task 1 ran all automated checks (all green). The app builds to APK. Now the final Phase 1 success criterion requires human verification: the app builds and launches offline with no runtime network request and no dangerous Android permissions.
  </what-built>
  <how-to-verify>
    1. Build the release APK:
       `flutter build apk --release`
       (Or use debug APK if release signing is not yet configured: `flutter build apk --debug`)

    2. Install on an Android device or emulator:
       `flutter install` (if device connected) or drag the APK to an emulator.

    3. Enable flight mode / airplane mode on the device.

    4. Launch the app from the home screen.

    5. Confirm:
       a. App launches without crashing (shows "Phase 1 Andamiaje" or "Phase 1 Scaffold" text — the ARB-backed placeholder)
       b. No "No internet connection" error dialog appears
       c. No crash or freeze within 10 seconds

    6. (Optional — Fiddler / Charles Proxy) Confirm no network traffic from the app. Not required for Phase 1 sign-off — the automated INTERNET permission check is sufficient.

    7. Check Play Protect / Android manifest using:
       `adb shell dumpsys package com.otis.brooke.flags.around.the.world | grep "permission"`
       Confirm NO dangerous permissions listed.
  </how-to-verify>
  <resume-signal>
    Type "phase 1 complete" if the app launches offline without crashing.
    Type "issue: [description]" if any problem is found.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| APK on device | Release APK signed and installed on Android device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-FINAL-01 | Information Disclosure | App makes network calls at runtime | mitigate | Task 1 CHECK 7 verifies no INTERNET permission in release manifest; Task 2 human verify confirms offline launch |
| T-1-FINAL-02 | Information Disclosure | Dangerous Android permissions | mitigate | adb shell dumpsys check in Task 2; manifest baseline established in Plan 02 |
| T-1-FINAL-03 | Tampering | npm/pip/cargo installs | mitigate | No new installs in this plan — verification only |
</threat_model>

<verification>
PHASE 1 SUCCESS CRITERIA (from ROADMAP.md — updated to 196 per D-09):
1. Python script produces valid `world_map_paths.json` with ISO code, SVG path string(s), bounding box, and centroid for all 196 countries. [CHECK 9]
2. Dart unit test loads `world_map_paths.json` from assets, parses it, asserts every country has non-empty ISO code and at least one path string — zero network calls. [CHECK 2]
3. `flutter gen-l10n` produces generated ARB-backed localization classes; all UI chrome strings externalized. [CHECK 4]
4. Dart test loads `countries_en.json` and asserts exactly 196 entries with non-empty names; adding `countries_es.json` causes data service to serve Spanish names without Dart code changes. [CHECK 2, CHECK 10, CHECK 11]
5. App builds and launches offline (flight-mode); no runtime network request attempted; app does not declare or request any dangerous Android permissions. [CHECK 7, CHECK 12, Task 2 human verify]
</verification>

<success_criteria>
All five Phase 1 success criteria satisfied:
- 196-country world_map_paths.json produced and verified (COMP-03)
- Dart test loads and parses JSON assets with zero network calls (COMP-03)
- flutter gen-l10n succeeds; ARB-backed UI strings (I18N-01)
- 196-entry countries_en.json and countries_es.json; locale swap works (I18N-02, I18N-03)
- App launches offline; no INTERNET in release manifest; no firebase (COMP-01, COMP-03)
Phase 1 is DONE.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-06-SUMMARY.md` when done
</output>
