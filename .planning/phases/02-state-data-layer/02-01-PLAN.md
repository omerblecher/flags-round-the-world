---
phase: 02-state-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/unit/game_session_notifier_test.dart
  - test/unit/high_score_repository_test.dart
  - test/unit/game_state_repository_test.dart
  - lib/features/game/game_phase.dart
  - lib/features/game/game_mode.dart
  - lib/features/game/game_session.dart
  - lib/core/ticker.dart
  - lib/core/data/high_score_repository.dart
  - lib/core/data/game_state_repository.dart
autonomous: true
requirements:
  - SCOR-01
  - SCOR-02
  - SCOR-04

must_haves:
  truths:
    - "Running flutter test test/unit/ exits 0 (all stubs pending, not erroring)"
    - "GameSession is an immutable value object with copyWith + equality"
    - "FakeTicker.tick() is callable from test code"
    - "HighScoreRepository and GameStateRepository abstract interfaces are exported"
    - "GamePhase and GameMode enums have exactly the specified values"
  artifacts:
    - path: "lib/features/game/game_phase.dart"
      provides: "GamePhase enum: idle, countdown, playing, paused, completed"
      contains: "enum GamePhase"
    - path: "lib/features/game/game_mode.dart"
      provides: "GameMode enum: learn, flagsMaster, geographicalMaster, grandMaster"
      contains: "enum GameMode"
    - path: "lib/features/game/game_session.dart"
      provides: "Immutable GameSession model with copyWith + == + hashCode"
      contains: "class GameSession"
    - path: "lib/core/ticker.dart"
      provides: "Ticker abstract class + RealTicker + FakeTicker with tick()"
      contains: "abstract class Ticker"
    - path: "lib/core/data/high_score_repository.dart"
      provides: "HighScoreRepository interface + SharedPreferencesHighScoreRepository"
      contains: "abstract interface class HighScoreRepository"
    - path: "lib/core/data/game_state_repository.dart"
      provides: "GameStateRepository interface + SharedPreferencesGameStateRepository"
      contains: "abstract interface class GameStateRepository"
    - path: "test/unit/game_session_notifier_test.dart"
      provides: "Failing test stubs for SC1, SC2, SCOR-01, SCOR-02"
      contains: "test('SC1:"
    - path: "test/unit/high_score_repository_test.dart"
      provides: "Failing test stubs for SC3, SCOR-04"
      contains: "test('SC3:"
    - path: "test/unit/game_state_repository_test.dart"
      provides: "Failing test stubs for SC4"
      contains: "test('SC4:"
  key_links:
    - from: "lib/features/game/game_session.dart"
      to: "lib/features/game/game_phase.dart"
      via: "import"
      pattern: "import.*game_phase"
    - from: "lib/features/game/game_session.dart"
      to: "lib/features/game/game_mode.dart"
      via: "import"
      pattern: "import.*game_mode"
    - from: "lib/core/data/high_score_repository.dart"
      to: "lib/features/game/game_mode.dart"
      via: "import"
      pattern: "import.*game_mode"
    - from: "lib/core/data/game_state_repository.dart"
      to: "lib/features/game/game_session.dart"
      via: "import"
      pattern: "import.*game_session"
---

<objective>
Establish the type contracts and test infrastructure that all Phase 2 implementation plans build against.

Purpose: In MVP vertical-slice mode, we need a failing test harness (RED state) before writing any implementation. This plan creates the test stubs and all domain type definitions so that Plan 02-02 and Plan 02-03 can implement against known contracts rather than discovering them at implementation time.

Output:
- 3 unit test stub files (failing with `fail('not implemented')` sentinels) covering SC1–SC4
- `GamePhase`, `GameMode`, `GameSession` — the core domain types consumed by every subsequent plan
- `Ticker` abstraction — injectable timer infrastructure (abstract class + `RealTicker` + `FakeTicker`)
- `HighScoreRepository` and `GameStateRepository` — abstract interfaces + complete SharedPreferences implementations
</objective>

<execution_context>
@/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-state-data-layer/02-CONTEXT.md
@.planning/phases/02-state-data-layer/02-RESEARCH.md

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from codebase. -->

From lib/features/ads/ad_load_state.dart — sealed class pattern to follow for GamePhase:
```dart
sealed class AdLoadState { const AdLoadState(); }
class AdLoaded extends AdLoadState { const AdLoaded(); }
class AdFailed extends AdLoadState { const AdFailed(); }
```
Note: GamePhase uses enum, not sealed class — but AdLoadState shows the "small domain type in its own file" convention.

From lib/features/ads/ad_service.dart — abstract interface + stub pattern to follow for repositories:
```dart
abstract interface class AdService {
  Future<AdLoadState> loadBannerAd();
}
class StubAdService implements AdService { const StubAdService(); ... }
```

From lib/core/models/country_data.dart — immutable model pattern:
```dart
class CountryData {
  final String isoCode;
  const CountryData({required this.isoCode, ...});
  factory CountryData.fromJson(Map<String, dynamic> json) { ... }
}
```

From lib/core/data/country_data_service.dart — service in lib/core/data/ convention.

From test/architecture/ads_isolation_test.dart — architecture guard (MUST stay green):
```dart
// Checks that lib/features/game/, lib/features/map/, lib/core/ have NO 'features/ads/' imports
if (content.contains('features/ads/')) { violations.add(entity.path); }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing test stub files (Wave 0 — RED state)</name>
  <files>
    test/unit/game_session_notifier_test.dart
    test/unit/high_score_repository_test.dart
    test/unit/game_state_repository_test.dart
  </files>
  <read_first>
    - test/unit/country_data_service_test.dart (existing test file — read for import style, group/test structure convention used in this project)
    - test/architecture/ads_isolation_test.dart (architecture test — must remain green after this task)
    - .planning/phases/02-state-data-layer/02-CONTEXT.md § Decisions D-07, D-12, D-13
    - .planning/phases/02-state-data-layer/02-RESEARCH.md § Pattern 6 (ProviderContainer.test) and Pattern 7 (SharedPreferences mock)
  </read_first>
  <behavior>
    - game_session_notifier_test.dart contains a group 'GameSessionNotifier' with:
      - test('SC1: idle → countdown → playing → paused → completed transitions')  → body calls fail('SC1 not implemented — RED state')
      - test('SC2: 30s + 3 errors = 18 points (golf-style, per D-13)')  → body calls fail('SC2 not implemented — RED state')
      - test('SCOR-01: score increments 1pt per 10s elapsed')  → fail('SCOR-01 not implemented — RED state')
      - test('SCOR-02: score increments 5pt per incorrect drop')  → fail('SCOR-02 not implemented — RED state')
    - high_score_repository_test.dart contains a group 'HighScoreRepository' with:
      - test('SC3: saves and reads back best score per GameMode')  → fail('SC3 not implemented — RED state')
      - test('SCOR-04: only updates when new score is lower (golf-style)')  → fail('SCOR-04 not implemented — RED state')
      - test('SC3: returns null when no score stored')  → fail('SC3 not implemented — RED state')
    - game_state_repository_test.dart contains a group 'GameStateRepository' with:
      - test('SC4: saveSession called once per correct drop')  → fail('SC4 not implemented — RED state')
      - test('SC4: saveSession serializes GameSession to SharedPreferences key game_session_snapshot')  → fail('SC4 not implemented — RED state')
  </behavior>
  <action>
    Create three test stub files. Each file starts with the necessary imports (flutter_test only — no game imports yet since the implementation files don't exist). Use `// ignore: unused_import` if the linter complains. The stub body for every test is exactly: `fail('description not implemented — RED state');` so `flutter test` reports test failures (not compilation errors).

    game_session_notifier_test.dart imports:
    - `package:flutter_test/flutter_test.dart`
    - `package:flutter_riverpod/flutter_riverpod.dart`
    - `package:flags_around_the_world/features/game/game_session_notifier.dart` (will be created in Plan 02-02 — add `// ignore: uri_does_not_exist` comment)
    - `package:flags_around_the_world/core/ticker.dart` (will be created in Task 2 of this plan)

    high_score_repository_test.dart imports:
    - `package:flutter_test/flutter_test.dart`
    - `package:shared_preferences/shared_preferences.dart`
    - `package:flags_around_the_world/core/data/high_score_repository.dart` (will be created in Task 2)

    game_state_repository_test.dart imports:
    - `package:flutter_test/flutter_test.dart`
    - `package:shared_preferences/shared_preferences.dart`
    - `package:mocktail/mocktail.dart`
    - `package:flags_around_the_world/core/data/game_state_repository.dart` (will be created in Task 2)
    - `package:flags_around_the_world/features/game/game_session.dart` (will be created in Task 2)

    Note: imports of files that don't yet exist are acceptable at stub creation time — the Dart analyzer will produce warnings but `flutter test` on existing tests still runs. The RED state is confirmed when Plan 02-02's implementation is in place and these tests compile fully.
  </action>
  <verify>
    <automated>cd C:/code/Claude/FlagsRoundTheWorld && flutter test test/architecture/ads_isolation_test.dart test/unit/country_data_service_test.dart test/unit/country_data_test.dart</automated>
  </verify>
  <done>
    - test/unit/game_session_notifier_test.dart exists with 4 test stubs calling fail()
    - test/unit/high_score_repository_test.dart exists with 3 test stubs calling fail()
    - test/unit/game_state_repository_test.dart exists with 2 test stubs calling fail()
    - Existing tests (ads_isolation_test.dart, country_data_*) still exit 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Domain type contracts — enums, GameSession model, Ticker, repository interfaces + impls</name>
  <files>
    lib/features/game/game_phase.dart
    lib/features/game/game_mode.dart
    lib/features/game/game_session.dart
    lib/core/ticker.dart
    lib/core/data/high_score_repository.dart
    lib/core/data/game_state_repository.dart
  </files>
  <read_first>
    - lib/features/ads/ad_load_state.dart (sealed class / single-concern file pattern)
    - lib/features/ads/ad_service.dart (abstract interface + impl in same file pattern)
    - lib/core/models/country_data.dart (immutable model with factory constructor pattern)
    - lib/core/data/country_data_service.dart (file placement in lib/core/data/)
    - .planning/phases/02-state-data-layer/02-CONTEXT.md §Decisions D-01 through D-13 (ALL decisions apply to this task)
    - .planning/phases/02-state-data-layer/02-RESEARCH.md §Pattern 3 (Ticker), §Pattern 4 (copyWith sentinel), §Pattern 5 (Repository)
  </read_first>
  <behavior>
    GamePhase enum — game_phase.dart contains exactly:
      enum GamePhase { idle, countdown, playing, paused, completed }
    
    GameMode enum — game_mode.dart contains exactly:
      enum GameMode { learn, flagsMaster, geographicalMaster, grandMaster }

    GameSession model — game_session.dart:
      - `const GameSession({required GamePhase phase, required GameMode mode, required int score, required Duration elapsed, required int errorCount, String? activeIsoCode, required int hintsRemaining})`
      - `copyWith(...)` uses the `Object? _sentinel = Object()` pattern for `activeIsoCode` so that `copyWith(activeIsoCode: null)` sets it to null rather than keeping the current value
      - `operator ==` checks all 7 fields
      - `hashCode` uses `Object.hash(phase, mode, score, elapsed, errorCount, activeIsoCode, hintsRemaining)`
      - No `fromJson`/`toJson` on the model itself — serialization lives in `GameStateRepository`

    Ticker — lib/core/ticker.dart contains three classes in one file (per D-09 + CONTEXT specifics):
      - `abstract class Ticker { void start(void Function() onTick); void stop(); }`
      - `class RealTicker implements Ticker` — wraps `Timer.periodic(const Duration(seconds: 1), (_) => _onTick!())`; field `void Function()? _onTick`; stop cancels timer
      - `class FakeTicker implements Ticker` — fields `void Function()? _onTick`; `start` stores callback; `stop` nulls it; exposes `void tick() => _onTick?.call()`

    HighScoreRepository — lib/core/data/high_score_repository.dart:
      - `abstract interface class HighScoreRepository { Future<int?> getBestScore(GameMode mode); Future<void> saveBestScore(GameMode mode, int score); }`
      - `class SharedPreferencesHighScoreRepository implements HighScoreRepository` with constructor `SharedPreferencesHighScoreRepository(this._prefs)` and `final SharedPreferences _prefs`
      - `static String _key(GameMode mode)` returns: learn→'high_score_learn', flagsMaster→'high_score_flags_master', geographicalMaster→'high_score_geographical_master', grandMaster→'high_score_grand_master' (switch expression, all cases)
      - `getBestScore` returns `_prefs.getInt(_key(mode))` (sync read, async wrapper)
      - `saveBestScore` only writes if `current == null || score < current` (golf-style: lower is better, per D-13)

    GameStateRepository — lib/core/data/game_state_repository.dart:
      - `abstract interface class GameStateRepository { Future<void> saveSession(GameSession session); Future<GameSession?> loadSession(); }`
      - `class SharedPreferencesGameStateRepository implements GameStateRepository` with constructor accepting `SharedPreferences _prefs`
      - `saveSession` serializes to flat JSON: `{'phase': session.phase.name, 'mode': session.mode.name, 'score': session.score, 'elapsedSeconds': session.elapsed.inSeconds, 'errorCount': session.errorCount, 'activeIsoCode': session.activeIsoCode, 'hintsRemaining': session.hintsRemaining}` — store with key `'game_session_snapshot'` via `_prefs.setString`
      - `loadSession` reads `'game_session_snapshot'` key, parses JSON, reconstructs `GameSession` using `GamePhase.values.byName(json['phase'])` and `GameMode.values.byName(json['mode'])`; returns `null` if key absent or parse fails

    Import constraints (CRITICAL — architecture test guard):
      - game_phase.dart: no imports
      - game_mode.dart: no imports
      - game_session.dart: imports game_phase.dart and game_mode.dart only (relative imports within features/game/)
      - ticker.dart: imports `dart:async` only
      - high_score_repository.dart: imports game_mode.dart and shared_preferences
      - game_state_repository.dart: imports game_session.dart and shared_preferences and dart:convert
      - NONE of these files may import anything from features/ads/ (enforced by ads_isolation_test.dart)
  </behavior>
  <action>
    Create the 6 files listed above. Follow the exact field names, enum values, method signatures, SharedPreferences keys, and sentinel pattern from the behavior block. The `_sentinel` field must be `static const Object _sentinel = Object()` declared inside `GameSession`. The `copyWith` signature for `activeIsoCode` is `Object? activeIsoCode = _sentinel` — inside the method body, check `activeIsoCode == _sentinel ? this.activeIsoCode : activeIsoCode as String?`.

    For `RealTicker`: import `dart:async`; field `Timer? _timer`; `start` assigns `_timer = Timer.periodic(...)` and stores the callback; `stop` calls `_timer?.cancel()` and sets `_timer = null`.

    For `SharedPreferencesGameStateRepository.loadSession`: wrap the parse in a try/catch; return `null` on any exception. Use `jsonDecode` from `dart:convert`.

    Do NOT use `@riverpod` annotations, `part` directives, `freezed`, or `equatable`. No code generation.
  </action>
  <verify>
    <automated>cd C:/code/Claude/FlagsRoundTheWorld && flutter test test/architecture/ads_isolation_test.dart test/unit/high_score_repository_test.dart test/unit/game_state_repository_test.dart</automated>
  </verify>
  <done>
    - All 6 files compile without errors (`flutter analyze lib/features/game/ lib/core/ticker.dart lib/core/data/` exits 0)
    - ads_isolation_test.dart exits 0 (no features/ads/ imports introduced)
    - high_score_repository_test.dart and game_state_repository_test.dart stubs compile and run (fail() bodies execute — tests fail, not error)
    - GameSession == operator: two instances with identical field values are equal
    - GameSession copyWith: `session.copyWith(activeIsoCode: null)` produces a session where activeIsoCode is null
    - HighScoreRepository saves only when new score is lower (golf-style)
  </done>
</task>

<task type="auto">
  <name>Task 3: Update test stubs with full implementations and confirm SC3 + SC4 green</name>
  <files>
    test/unit/high_score_repository_test.dart
    test/unit/game_state_repository_test.dart
  </files>
  <read_first>
    - lib/core/data/high_score_repository.dart (just created in Task 2 — read actual signatures)
    - lib/core/data/game_state_repository.dart (just created in Task 2 — read actual signatures)
    - lib/features/game/game_session.dart (just created — read constructor signature)
    - lib/features/game/game_phase.dart (enum values)
    - lib/features/game/game_mode.dart (enum values)
    - .planning/phases/02-state-data-layer/02-RESEARCH.md §Pattern 7 (SharedPreferences mock), §Code Examples SC3+SC4 test
  </read_first>
  <behavior>
    high_score_repository_test.dart — replace fail() stubs with real assertions:
      - setUp: `SharedPreferences.setMockInitialValues({})` then `prefs = await SharedPreferences.getInstance()` then `repo = SharedPreferencesHighScoreRepository(prefs)`
      - 'SC3: returns null when no score stored': `expect(await repo.getBestScore(GameMode.learn), isNull)`
      - 'SC3: saves and reads back best score per GameMode': `await repo.saveBestScore(GameMode.learn, 18); expect(await repo.getBestScore(GameMode.learn), 18)`
      - 'SCOR-04: only updates when new score is lower (golf-style)': save 18, save 25 (should NOT overwrite), assert still 18; then save 12 (SHOULD overwrite), assert 12

    game_state_repository_test.dart — replace fail() stubs:
      - setUp: `SharedPreferences.setMockInitialValues({})` then `prefs = await SharedPreferences.getInstance()` then `repo = SharedPreferencesGameStateRepository(prefs)`
      - helper: construct a `GameSession` with `phase: GamePhase.playing, mode: GameMode.learn, score: 18, elapsed: Duration(seconds: 30), errorCount: 3, activeIsoCode: 'de', hintsRemaining: 2`
      - 'SC4: saveSession serializes to game_session_snapshot key': call `repo.saveSession(session)`, then read raw string from prefs at key 'game_session_snapshot', decode JSON, assert `json['phase'] == 'playing'` and `json['score'] == 18`
      - 'SC4: loadSession round-trips GameSession correctly': save then load, assert loaded session equals original (relies on GameSession ==)

    Note: SC4 mock-write-count test (verify saveSession called N times) lives in game_session_notifier_test.dart (Plan 02-02), not here. The game_state_repository_test.dart tests only the repository's own serialization behavior.
  </behavior>
  <action>
    Replace the `fail(...)` stub bodies in both test files with the real test implementations described in the behavior block. Preserve the group name and test name strings exactly (they are the acceptance criteria identifiers). Add `setUp` and any needed `late` variable declarations at group scope. Use `async/await` throughout.

    Import `dart:convert` in game_state_repository_test.dart for JSON decode verification.

    After replacing stubs, run the test suite to confirm SC3 and SC4 are green. Do NOT modify game_session_notifier_test.dart in this task — those stubs stay as `fail()` until Plan 02-02.
  </action>
  <verify>
    <automated>cd C:/code/Claude/FlagsRoundTheWorld && flutter test test/unit/high_score_repository_test.dart test/unit/game_state_repository_test.dart test/architecture/ads_isolation_test.dart</automated>
  </verify>
  <done>
    - flutter test test/unit/high_score_repository_test.dart exits 0 with 3 tests passing
    - flutter test test/unit/game_state_repository_test.dart exits 0 with 2 tests passing
    - ads_isolation_test.dart still exits 0
    - SCOR-04 and SC3 success criteria from VALIDATION.md are verified green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SharedPreferences read boundary | Data read from device storage at app launch; could be corrupt or missing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | SharedPreferences (score storage) | accept | Scores are non-sensitive game data; no user PII; a tampered score affects only that user's local leaderboard. No server-side validation needed at this phase. |
| T-02-02 | Denial of Service | GameStateRepository.loadSession | mitigate | Wrap JSON parse in try/catch; return null on corrupt data so the app degrades gracefully rather than crashing on relaunch |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 2; all packages carried forward from Phase 1 legitimacy audit |
</threat_model>

<verification>
After all tasks complete:

1. `flutter test test/unit/high_score_repository_test.dart` — exits 0 (3 tests green)
2. `flutter test test/unit/game_state_repository_test.dart` — exits 0 (2 tests green)
3. `flutter test test/architecture/ads_isolation_test.dart` — exits 0
4. `flutter analyze lib/features/game/ lib/core/ticker.dart lib/core/data/` — no errors
5. `grep -rn "features/ads/" lib/features/game/ lib/core/` — returns no matches
6. `flutter test test/unit/ test/architecture/` — architecture + repo tests green; notifier stubs still fail (expected — Plan 02-02 not yet executed)
</verification>

<success_criteria>
- GamePhase enum: 5 values — idle, countdown, playing, paused, completed
- GameMode enum: 4 values — learn, flagsMaster, geographicalMaster, grandMaster
- GameSession: immutable, all 7 fields final, copyWith handles activeIsoCode: null correctly, == and hashCode implemented
- Ticker: abstract class + RealTicker (Timer.periodic) + FakeTicker (tick() callable from tests)
- HighScoreRepository: abstract interface + SharedPreferencesHighScoreRepository; golf-style save (lower wins); 4 SharedPreferences keys per D-09 CONTEXT specifics
- GameStateRepository: abstract interface + SharedPreferencesGameStateRepository; flat JSON to 'game_session_snapshot'; round-trips correctly
- All 5 repository tests green; notifier test stubs in fail() state (RED — correct)
- ads_isolation_test.dart green
</success_criteria>

<output>
Create `.planning/phases/02-state-data-layer/02-01-SUMMARY.md` when done
</output>
