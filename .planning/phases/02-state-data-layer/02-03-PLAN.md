---
phase: 02-state-data-layer
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - test/unit/game_session_notifier_test.dart
autonomous: true
requirements:
  - SCOR-01
  - SCOR-02
  - SCOR-04

must_haves:
  truths:
    - "flutter test exits 0 for all unit tests and architecture tests"
    - "SC1 through SC4 all pass (full phase success criteria green)"
    - "SCOR-01, SCOR-02, SCOR-04 verified by automated tests"
    - "ads_isolation_test.dart exits 0"
    - "flutter analyze exits 0 on lib/"
  artifacts:
    - path: "test/unit/game_session_notifier_test.dart"
      provides: "All 5 notifier tests green including SC4 correct-drop write-count"
      contains: "verify(() => mockRepo.saveSession(any())).called(2)"
  key_links:
    - from: "test/unit/game_session_notifier_test.dart"
      to: "lib/features/game/game_session_notifier.dart"
      via: "ProviderContainer.test override with MockGameStateRepository"
      pattern: "MockGameStateRepository"
    - from: "test/unit/game_session_notifier_test.dart"
      to: "lib/core/data/game_state_repository.dart"
      via: "mocktail Mock class"
      pattern: "implements GameStateRepository"
---

<objective>
Phase-end integration checkpoint: confirm all Phase 2 success criteria are green in a single test run, and run full flutter analyze.

Purpose: Plans 02-01 and 02-02 deliver implementations; this plan verifies they compose correctly. The SC4 mock-count test for `GameStateRepository` writes may have been added in Plan 02-02 but this plan ensures its final state is correct and that the full test suite (unit + architecture) exits 0 before the phase is handed to `/gsd:verify-work`.

Output:
- `test/unit/game_session_notifier_test.dart` — final verified state with all tests green
- Phase 2 verification commands confirmed passing
</objective>

<execution_context>
@/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-state-data-layer/02-01-SUMMARY.md
@.planning/phases/02-state-data-layer/02-02-SUMMARY.md
@.planning/phases/02-state-data-layer/02-CONTEXT.md
@.planning/phases/02-state-data-layer/02-VALIDATION.md

<interfaces>
<!-- All contracts from Plans 02-01 and 02-02 should now be implemented. -->

From lib/features/game/game_session_notifier.dart (from Plan 02-02):
  - GameSessionNotifier constructor: GameSessionNotifier({required Ticker ticker, GameStateRepository? gameStateRepository, HighScoreRepository? highScoreRepository})
  - gameSessionProvider top-level provider
  - Methods: startGame(GameMode), pauseGame(), resumeGame(), recordDrop(String, {required bool isCorrect}), completeGame()
  - Getter: countdownSecondsRemaining

From lib/core/data/game_state_repository.dart (from Plan 02-01):
  - abstract interface class GameStateRepository { Future<void> saveSession(GameSession session); Future<GameSession?> loadSession(); }

From lib/core/data/high_score_repository.dart (from Plan 02-01):
  - abstract interface class HighScoreRepository { Future<int?> getBestScore(GameMode mode); Future<void> saveBestScore(GameMode mode, int score); }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final integration check — run full test suite and analyze</name>
  <files>
    test/unit/game_session_notifier_test.dart
  </files>
  <read_first>
    - test/unit/game_session_notifier_test.dart (read current state — confirm SC4 mock test is present and correct)
    - lib/features/game/game_session_notifier.dart (read to confirm recordDrop writes repo, no features/ads/ imports)
    - .planning/phases/02-state-data-layer/02-VALIDATION.md (phase gate checklist)
    - .planning/phases/02-state-data-layer/02-CONTEXT.md §SC4 (confirm mock write count assertion is correct)
  </read_first>
  <action>
    Run the verification commands below in sequence. If any test fails, diagnose and fix the specific failing test or implementation file before re-running. Do NOT proceed to the next command until the current one exits 0.

    Common failure modes to check:
    - SC4 test: if `verify(...).called(2)` fails, check that `recordDrop` in game_session_notifier.dart calls `_gameStateRepository?.saveSession(current)` when isCorrect is true — not deferred to completeGame
    - SC2 test: if score assertion fails with 8 instead of 18, the scoring formula is wrong — must be `(elapsedSeconds ~/ 10) + (errorCount * 5)`, not just error points
    - ads_isolation_test: if it fails, a new import from features/ads/ was accidentally added — scan game_session_notifier.dart imports
    - SharedPreferences leak: if repository tests fail intermittently, ensure `SharedPreferences.setMockInitialValues({})` is in setUp (not setUpAll)

    If game_session_notifier_test.dart is missing the SC4 mock test (was not added in Plan 02-02), add it now:
    - Declare `class MockGameStateRepository extends Mock implements GameStateRepository {}`  at file top
    - Add test to the group: 'SC4: saveSession called once per correct drop'
    - Set up: `final mockRepo = MockGameStateRepository()`, `when(() => mockRepo.saveSession(any())).thenAnswer((_) async {})`
    - Create container with overrideWith providing `GameSessionNotifier(ticker: fakeTicker, gameStateRepository: mockRepo)`
    - Start game, advance 3 countdown ticks, call `recordDrop('de', isCorrect: true)` and `recordDrop('fr', isCorrect: true)`
    - Assert `verify(() => mockRepo.saveSession(any())).called(2)`

    If game_session_notifier_test.dart already has this test and it is green — no file modification needed; this task is verification-only.
  </action>
  <verify>
    <automated>cd C:/code/Claude/FlagsRoundTheWorld && flutter test test/unit/ test/architecture/ && flutter analyze lib/</automated>
  </verify>
  <done>
    - flutter test test/unit/ test/architecture/ exits 0
    - flutter analyze lib/ exits 0 with no errors
    - SC1, SC2, SC3, SC4, SCOR-01, SCOR-02, SCOR-04 all verified green
    - ads_isolation_test.dart exits 0
    - Total passing tests: 5 notifier + 3 high_score_repo + 2 game_state_repo + 2 existing = 12 tests
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 2 complete state machine: GameSessionNotifier (AsyncNotifier, injectable Ticker, no ads imports), golf-style scoring (SCOR-01 + SCOR-02), HighScoreRepository + GameStateRepository (SharedPreferences, golf-style persistence), full unit test coverage across SC1–SC4 and SCOR-01/SCOR-02/SCOR-04. 12 tests passing, 0 failures.
  </what-built>
  <how-to-verify>
    Run from the project root:

    1. `flutter test test/unit/ test/architecture/` — should print "All tests passed." with 12 tests
    2. `flutter test` — full suite; no regressions in widget_test.dart or other existing tests
    3. `flutter analyze lib/` — "No issues found!" or only INFO-level lints
    4. Optional spot check: open lib/features/game/game_session_notifier.dart and confirm:
       - Line 1 has no import containing 'features/ads/'
       - A top-level `gameSessionProvider` is declared
       - The constructor accepts `Ticker ticker` as a required named parameter
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to `/gsd:verify-work 2`, or describe any test failures to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite output | Phase gate: all tests must pass before verify-work handoff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Test isolation (SharedPreferences singleton leak) | mitigate | Verify setUp (not setUpAll) pattern is used in all repository tests; failing tests on re-run is the signal |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 2 |
</threat_model>

<verification>
Phase 2 gate (all must pass before `/gsd:verify-work 2`):

1. `flutter test test/unit/ test/architecture/` — exits 0
2. `flutter test` — exits 0 (full suite including widget_test.dart)
3. `flutter analyze lib/` — exits 0 (no errors)
4. `grep -rn "features/ads/" lib/features/game/ lib/core/` — zero matches
5. Manual spot check: game_session_notifier.dart has no @riverpod annotation and no part directive
</verification>

<success_criteria>
Phase 2 is complete when:
- All 4 ROADMAP Phase 2 success criteria are green (SC1–SC4)
- SCOR-01: 1 point per 10 seconds confirmed by automated test
- SCOR-02: 5 points per incorrect drop confirmed by automated test
- SCOR-04: Lowest-score persistence per GameMode confirmed by automated test
- GameSessionNotifier has zero imports from features/ads/ (architecture test green)
- flutter analyze lib/ exits 0
- Phase 3 can import gameSessionProvider and GameSession without any Phase 2 changes
</success_criteria>

<output>
Create `.planning/phases/02-state-data-layer/02-03-SUMMARY.md` when done
</output>
