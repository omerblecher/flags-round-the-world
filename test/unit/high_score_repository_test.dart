import 'package:flutter_test/flutter_test.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:flags_around_the_world/core/data/high_score_repository.dart';

void main() {
  group('HighScoreRepository', () {
    test('SC3: saves and reads back best score per GameMode', () {
      fail('SC3 not implemented — RED state');
    });

    test('SCOR-04: only updates when new score is lower (golf-style)', () {
      fail('SCOR-04 not implemented — RED state');
    });

    test('SC3: returns null when no score stored', () {
      fail('SC3 not implemented — RED state');
    });
  });
}
