import 'package:flutter_test/flutter_test.dart';
// ignore: unused_import
import 'package:flutter_riverpod/flutter_riverpod.dart';

void main() {
  group('DragTarget callbacks', () {
    test(
      'MAP-05: hover state updates to correct isoCode on drag-over',
      () {},
      skip: 'TODO Phase 4: requires mock notifier wiring',
    );
    test(
      'GAME-01: recordDrop called with isCorrect:true on correct country drop',
      () {},
      skip: 'TODO Phase 4: requires mock notifier wiring',
    );
    test(
      'GAME-03/04: recordDrop called with isCorrect:false on wrong country drop',
      () {},
      skip: 'TODO Phase 4: requires mock notifier wiring',
    );
  });
}
